/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type DeploymentStats = {
  /**
   * Total number of deployments matching filters
   */
  total: number;
  /**
   * Count of deployments by status (only includes statuses with non-zero counts)
   */
  byStatus: { [k: string]: number };
  /**
   * Count of deployments by platform (only includes platforms with non-zero counts)
   */
  byPlatform: { [k: string]: number };
};

/** @internal */
export const DeploymentStats$inboundSchema: z.ZodType<
  DeploymentStats,
  unknown
> = z.object({
  total: z.number(),
  byStatus: z.record(z.string(), z.number()),
  byPlatform: z.record(z.string(), z.number()),
});

export function deploymentStatsFromJSON(
  jsonString: string,
): SafeParseResult<DeploymentStats, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => DeploymentStats$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'DeploymentStats' from JSON`,
  );
}
