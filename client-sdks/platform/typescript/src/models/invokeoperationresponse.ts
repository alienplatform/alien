/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4";
import { safeParse } from "../lib/schemas.js";
import { ClosedEnum } from "../types/enums.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

/**
 * The operation's declared risk tier (informational).
 */
export const InvokeOperationResponseTier = {
  ReadOnly: "read-only",
  Mutating: "mutating",
  Destructive: "destructive",
} as const;
/**
 * The operation's declared risk tier (informational).
 */
export type InvokeOperationResponseTier = ClosedEnum<
  typeof InvokeOperationResponseTier
>;

/**
 * The approval decision the policy resolved to.
 */
export const InvokeOperationResponseDecision = {
  Auto: "auto",
  Manual: "manual",
} as const;
/**
 * The approval decision the policy resolved to.
 */
export type InvokeOperationResponseDecision = ClosedEnum<
  typeof InvokeOperationResponseDecision
>;

/**
 * dispatched: auto-approved and running. pending-approval: needs customer sign-off.
 */
export const InvokeOperationResponseStatus = {
  Dispatched: "dispatched",
  PendingApproval: "pending-approval",
} as const;
/**
 * dispatched: auto-approved and running. pending-approval: needs customer sign-off.
 */
export type InvokeOperationResponseStatus = ClosedEnum<
  typeof InvokeOperationResponseStatus
>;

export type InvokeOperationResponse = {
  plugin: string;
  operation: string;
  /**
   * The operation's declared risk tier (informational).
   */
  tier: InvokeOperationResponseTier;
  /**
   * The approval decision the policy resolved to.
   */
  decision: InvokeOperationResponseDecision;
  /**
   * dispatched: auto-approved and running. pending-approval: needs customer sign-off.
   */
  status: InvokeOperationResponseStatus;
  /**
   * The created operation-command id when dispatched (absent when pending).
   */
  commandId?: string | undefined;
};

/** @internal */
export const InvokeOperationResponseTier$inboundSchema: z.ZodEnum<
  typeof InvokeOperationResponseTier
> = z.enum(InvokeOperationResponseTier);

/** @internal */
export const InvokeOperationResponseDecision$inboundSchema: z.ZodEnum<
  typeof InvokeOperationResponseDecision
> = z.enum(InvokeOperationResponseDecision);

/** @internal */
export const InvokeOperationResponseStatus$inboundSchema: z.ZodEnum<
  typeof InvokeOperationResponseStatus
> = z.enum(InvokeOperationResponseStatus);

/** @internal */
export const InvokeOperationResponse$inboundSchema: z.ZodType<
  InvokeOperationResponse,
  unknown
> = z.object({
  plugin: z.string(),
  operation: z.string(),
  tier: InvokeOperationResponseTier$inboundSchema,
  decision: InvokeOperationResponseDecision$inboundSchema,
  status: InvokeOperationResponseStatus$inboundSchema,
  commandId: z.string().optional(),
});

export function invokeOperationResponseFromJSON(
  jsonString: string,
): SafeParseResult<InvokeOperationResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => InvokeOperationResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'InvokeOperationResponse' from JSON`,
  );
}
