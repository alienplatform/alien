/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type BillingFeatureFlags = {
  customDomains: boolean;
  privateManagers: boolean;
  ssoSaml: boolean;
  auditLogs: boolean;
  airgapped: boolean;
};

/** @internal */
export const BillingFeatureFlags$inboundSchema: z.ZodType<
  BillingFeatureFlags,
  unknown
> = z.object({
  custom_domains: z.boolean(),
  private_managers: z.boolean(),
  sso_saml: z.boolean(),
  audit_logs: z.boolean(),
  airgapped: z.boolean(),
}).transform((v) => {
  return remap$(v, {
    "custom_domains": "customDomains",
    "private_managers": "privateManagers",
    "sso_saml": "ssoSaml",
    "audit_logs": "auditLogs",
  });
});

export function billingFeatureFlagsFromJSON(
  jsonString: string,
): SafeParseResult<BillingFeatureFlags, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => BillingFeatureFlags$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'BillingFeatureFlags' from JSON`,
  );
}
