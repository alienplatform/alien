/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type SetupFingerprintInfo = {
  /**
   * Stable target key for the setup contract, e.g. aws/us-east-1
   */
  target: string;
  /**
   * Deterministic setup contract fingerprint for one setup target
   */
  fingerprint: string;
  /**
   * Setup fingerprint algorithm version
   */
  version: number;
};

/** @internal */
export const SetupFingerprintInfo$inboundSchema: z.ZodType<
  SetupFingerprintInfo,
  unknown
> = z.object({
  target: z.string(),
  fingerprint: z.string(),
  version: z.int(),
});

export function setupFingerprintInfoFromJSON(
  jsonString: string,
): SafeParseResult<SetupFingerprintInfo, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SetupFingerprintInfo$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SetupFingerprintInfo' from JSON`,
  );
}
