/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type Membership = {
  /**
   * Unique identifier for the workspace.
   */
  id: string;
  name: string;
  logoUrl: string | null;
  role: string;
  createdAt: Date;
};

/** @internal */
export const Membership$inboundSchema: z.ZodType<Membership, unknown> = z
  .object({
    id: z.string(),
    name: z.string(),
    logoUrl: z.nullable(z.string()),
    role: z.string(),
    createdAt: z.iso.datetime({ offset: true }).transform(v => new Date(v)),
  });

export function membershipFromJSON(
  jsonString: string,
): SafeParseResult<Membership, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Membership$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Membership' from JSON`,
  );
}
