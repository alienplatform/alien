/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4"
import { safeParse } from "../lib/schemas.js"
import type { ClosedEnum } from "../types/enums.js"
import type { Result as SafeParseResult } from "../types/fp.js"
import type { SDKValidationError } from "./errors/sdkvalidationerror.js"

/**
 * Type of animated background to display on the deployment page.
 */
export const DeploymentPageBackgroundType = {
  GradientMesh: "gradient-mesh",
  FloatingOrbs: "floating-orbs",
  FlickeringGrid: "flickering-grid",
  BubbleGlow: "bubble-glow",
  ParticleField: "particle-field",
} as const
/**
 * Type of animated background to display on the deployment page.
 */
export type DeploymentPageBackgroundType = ClosedEnum<typeof DeploymentPageBackgroundType>

/**
 * Color mode for the background animation.
 */
export const DeploymentPageBackgroundMode = {
  Dark: "dark",
  Light: "light",
} as const
/**
 * Color mode for the background animation.
 */
export type DeploymentPageBackgroundMode = ClosedEnum<typeof DeploymentPageBackgroundMode>

/**
 * Color scheme for the background animation.
 */
export const DeploymentPageBackgroundColorScheme = {
  Blue: "blue",
  Purple: "purple",
  Green: "green",
  Orange: "orange",
  Pink: "pink",
} as const
/**
 * Color scheme for the background animation.
 */
export type DeploymentPageBackgroundColorScheme = ClosedEnum<
  typeof DeploymentPageBackgroundColorScheme
>

export type DeploymentPageBackground = {
  /**
   * Type of animated background to display on the deployment page.
   */
  type: DeploymentPageBackgroundType
  /**
   * Color mode for the background animation.
   */
  mode: DeploymentPageBackgroundMode
  /**
   * Color scheme for the background animation.
   */
  colorScheme: DeploymentPageBackgroundColorScheme
}

/** @internal */
export const DeploymentPageBackgroundType$inboundSchema: z.ZodEnum<
  typeof DeploymentPageBackgroundType
> = z.enum(DeploymentPageBackgroundType)

/** @internal */
export const DeploymentPageBackgroundMode$inboundSchema: z.ZodEnum<
  typeof DeploymentPageBackgroundMode
> = z.enum(DeploymentPageBackgroundMode)

/** @internal */
export const DeploymentPageBackgroundColorScheme$inboundSchema: z.ZodEnum<
  typeof DeploymentPageBackgroundColorScheme
> = z.enum(DeploymentPageBackgroundColorScheme)

/** @internal */
export const DeploymentPageBackground$inboundSchema: z.ZodType<DeploymentPageBackground, unknown> =
  z.object({
    type: DeploymentPageBackgroundType$inboundSchema,
    mode: DeploymentPageBackgroundMode$inboundSchema,
    colorScheme: DeploymentPageBackgroundColorScheme$inboundSchema,
  })

export function deploymentPageBackgroundFromJSON(
  jsonString: string,
): SafeParseResult<DeploymentPageBackground, SDKValidationError> {
  return safeParse(
    jsonString,
    x => DeploymentPageBackground$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'DeploymentPageBackground' from JSON`,
  )
}
