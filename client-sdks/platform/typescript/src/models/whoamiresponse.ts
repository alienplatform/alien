/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";
import {
  ServiceAccountSubject,
  ServiceAccountSubject$inboundSchema,
} from "./serviceaccountsubject.js";
import { UserRole, UserRole$inboundSchema } from "./userrole.js";

/**
 * Authenticated user subject with workspace-scoped permissions
 */
export type WhoamiResponseUser = {
  /**
   * Subject type identifier
   */
  kind: "user";
  /**
   * Unique user identifier
   */
  id: string;
  /**
   * User's email address
   */
  email: string;
  /**
   * ID of the workspace the user is authenticated within
   */
  workspaceId: string;
  /**
   * Name of the workspace the user is authenticated within
   */
  workspaceName?: string | undefined;
  /**
   * User's role within the workspace
   */
  role: UserRole;
  /**
   * Whether this user has ever authenticated a request from the Alien CLI. Latched on first CLI request, never cleared.
   */
  cliConnected: boolean;
  /**
   * When the Getting Started walkthrough was dismissed or completed for the current workspace. Null until dismissed; the dashboard auto-promotes the walkthrough while this is null.
   */
  onboardingDismissedAt: Date | null;
};

/**
 * Whoami endpoint response — the authenticated subject plus onboarding flags (user only).
 */
export type WhoamiResponse = WhoamiResponseUser | ServiceAccountSubject;

/** @internal */
export const WhoamiResponseUser$inboundSchema: z.ZodType<
  WhoamiResponseUser,
  unknown
> = z.object({
  kind: z.literal("user"),
  id: z.string(),
  email: z.string(),
  workspaceId: z.string(),
  workspaceName: z.string().optional(),
  role: UserRole$inboundSchema,
  cliConnected: z.boolean(),
  onboardingDismissedAt: z.nullable(
    z.iso.datetime({ offset: true }).transform(v => new Date(v)),
  ),
});

export function whoamiResponseUserFromJSON(
  jsonString: string,
): SafeParseResult<WhoamiResponseUser, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => WhoamiResponseUser$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'WhoamiResponseUser' from JSON`,
  );
}

/** @internal */
export const WhoamiResponse$inboundSchema: z.ZodType<WhoamiResponse, unknown> =
  z.union([
    z.lazy(() => WhoamiResponseUser$inboundSchema),
    ServiceAccountSubject$inboundSchema,
  ]);

export function whoamiResponseFromJSON(
  jsonString: string,
): SafeParseResult<WhoamiResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => WhoamiResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'WhoamiResponse' from JSON`,
  );
}
