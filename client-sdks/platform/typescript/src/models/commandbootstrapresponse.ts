/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import {
  CommandReceiverBootstrapTarget,
  CommandReceiverBootstrapTarget$inboundSchema,
} from "./commandreceiverbootstraptarget.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type CommandBootstrapResponse = {
  /**
   * Current customer-facing manager URL
   */
  managerUrl: string;
  /**
   * Short-lived command capability token
   */
  token: string;
  /**
   * Capability expiry in RFC 3339 format
   */
  expiresAt: Date;
  /**
   * Resolved target identity; present only for receiver bootstrap
   */
  target?: CommandReceiverBootstrapTarget | undefined;
};

/** @internal */
export const CommandBootstrapResponse$inboundSchema: z.ZodType<
  CommandBootstrapResponse,
  unknown
> = z.object({
  managerUrl: z.string(),
  token: z.string(),
  expiresAt: z.iso.datetime({ offset: true }).transform(v => new Date(v)),
  target: CommandReceiverBootstrapTarget$inboundSchema.optional(),
});

export function commandBootstrapResponseFromJSON(
  jsonString: string,
): SafeParseResult<CommandBootstrapResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CommandBootstrapResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CommandBootstrapResponse' from JSON`,
  );
}
