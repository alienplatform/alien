/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4";
import { safeParse } from "../lib/schemas.js";
import { ClosedEnum } from "../types/enums.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export const SessionStatus = {
  Queued: "queued",
  Running: "running",
  Halted: "halted",
  Complete: "complete",
  Error: "error",
  Canceled: "canceled",
} as const;
export type SessionStatus = ClosedEnum<typeof SessionStatus>;

export type Incident = {
  id: string;
  number: number;
  reference: string;
  title: string;
  scopeLabel: string | null;
  agentSessionId: string | null;
  sessionStatus: SessionStatus | null;
  projectId: string | null;
  deploymentId: string | null;
  releaseId: string | null;
  createdAt: Date;
  updatedAt: Date;
};

/** @internal */
export const SessionStatus$inboundSchema: z.ZodEnum<typeof SessionStatus> = z
  .enum(SessionStatus);

/** @internal */
export const Incident$inboundSchema: z.ZodType<Incident, unknown> = z.object({
  id: z.string(),
  number: z.int(),
  reference: z.string(),
  title: z.string(),
  scopeLabel: z.nullable(z.string()),
  agentSessionId: z.nullable(z.string()),
  sessionStatus: z.nullable(SessionStatus$inboundSchema),
  projectId: z.nullable(z.string()),
  deploymentId: z.nullable(z.string()),
  releaseId: z.nullable(z.string()),
  createdAt: z.iso.datetime({ offset: true }).transform(v => new Date(v)),
  updatedAt: z.iso.datetime({ offset: true }).transform(v => new Date(v)),
});

export function incidentFromJSON(
  jsonString: string,
): SafeParseResult<Incident, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Incident$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Incident' from JSON`,
  );
}
