/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4";

export type RetryManagerRequest = {
  /**
   * Unique identifier for a manager.
   */
  id: string;
  /**
   * Workspace name. Defaults to your last workspace (user auth) or your API key's workspace (token auth). When using an API key, if provided, must match the key's workspace.
   */
  workspace?: string | undefined;
};

/** @internal */
export type RetryManagerRequest$Outbound = {
  id: string;
  workspace?: string | undefined;
};

/** @internal */
export const RetryManagerRequest$outboundSchema: z.ZodType<
  RetryManagerRequest$Outbound,
  RetryManagerRequest
> = z.object({
  id: z.string(),
  workspace: z.string().optional(),
});

export function retryManagerRequestToJSON(
  retryManagerRequest: RetryManagerRequest,
): string {
  return JSON.stringify(
    RetryManagerRequest$outboundSchema.parse(retryManagerRequest),
  );
}
