/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4";
import { ClosedEnum } from "../../types/enums.js";

/**
 * Target platform to resolve the manager for
 */
export const ResolvePlatform = {
  Aws: "aws",
  Gcp: "gcp",
  Azure: "azure",
  Kubernetes: "kubernetes",
  Machines: "machines",
  Local: "local",
  Test: "test",
} as const;
/**
 * Target platform to resolve the manager for
 */
export type ResolvePlatform = ClosedEnum<typeof ResolvePlatform>;

export type ResolveRequest = {
  /**
   * Workspace name. Required for user/session/OAuth requests. Optional for API keys because API keys are workspace-scoped; if provided with an API key, it must match the key's workspace.
   */
  workspace?: string | undefined;
  /**
   * Target platform to resolve the manager for
   */
  platform: ResolvePlatform;
  /**
   * Project ID or name. Required for user and workspace-scoped tokens. Optional for project/deployment-group/deployment-scoped tokens (derived from token scope).
   */
  project?: string | undefined;
};

/** @internal */
export const ResolvePlatform$outboundSchema: z.ZodEnum<typeof ResolvePlatform> =
  z.enum(ResolvePlatform);

/** @internal */
export type ResolveRequest$Outbound = {
  workspace?: string | undefined;
  platform: string;
  project?: string | undefined;
};

/** @internal */
export const ResolveRequest$outboundSchema: z.ZodType<
  ResolveRequest$Outbound,
  ResolveRequest
> = z.object({
  workspace: z.string().optional(),
  platform: ResolvePlatform$outboundSchema,
  project: z.string().optional(),
});

export function resolveRequestToJSON(resolveRequest: ResolveRequest): string {
  return JSON.stringify(ResolveRequest$outboundSchema.parse(resolveRequest));
}
