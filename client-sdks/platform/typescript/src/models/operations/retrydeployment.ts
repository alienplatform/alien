/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type RetryDeploymentRequest = {
  /**
   * Unique identifier for the deployment.
   */
  id: string;
  /**
   * Workspace name. Defaults to your last workspace (user auth) or your API key's workspace (token auth). When using an API key, if provided, must match the key's workspace.
   */
  workspace?: string | undefined;
};

/**
 * Agent retry enqueued successfully.
 */
export type RetryDeploymentResponse = {
  message: string;
};

/** @internal */
export type RetryDeploymentRequest$Outbound = {
  id: string;
  workspace?: string | undefined;
};

/** @internal */
export const RetryDeploymentRequest$outboundSchema: z.ZodType<
  RetryDeploymentRequest$Outbound,
  RetryDeploymentRequest
> = z.object({
  id: z.string(),
  workspace: z.string().optional(),
});

export function retryDeploymentRequestToJSON(
  retryDeploymentRequest: RetryDeploymentRequest,
): string {
  return JSON.stringify(
    RetryDeploymentRequest$outboundSchema.parse(retryDeploymentRequest),
  );
}

/** @internal */
export const RetryDeploymentResponse$inboundSchema: z.ZodType<
  RetryDeploymentResponse,
  unknown
> = z.object({
  message: z.string(),
});

export function retryDeploymentResponseFromJSON(
  jsonString: string,
): SafeParseResult<RetryDeploymentResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => RetryDeploymentResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'RetryDeploymentResponse' from JSON`,
  );
}
