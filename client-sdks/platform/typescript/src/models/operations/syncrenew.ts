/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import * as models from "../index.js";

export type SyncRenewRequest = {
  /**
   * Workspace name. Required for user/session/OAuth requests. Optional for API keys because API keys are workspace-scoped; if provided with an API key, it must match the key's workspace.
   */
  workspace?: string | undefined;
  syncRenewRequest: models.SyncRenewRequest;
};

export type Lease = {
  /**
   * Unique identifier for the deployment.
   */
  deploymentId: string;
  /**
   * False when this session no longer holds a live lease.
   */
  renewed: boolean;
  leaseExpiresAt: Date | null;
};

/**
 * Renewal processed. Inspect `leases` for per-deployment outcomes; a lost lease is reported there rather than failing the whole batch.
 */
export type SyncRenewResponse = {
  /**
   * True when every requested deployment was renewed.
   */
  success: boolean;
  /**
   * Per-deployment renewal outcome.
   */
  leases: Array<Lease>;
};

/** @internal */
export type SyncRenewRequest$Outbound = {
  workspace?: string | undefined;
  SyncRenewRequest: models.SyncRenewRequest$Outbound;
};

/** @internal */
export const SyncRenewRequest$outboundSchema: z.ZodType<
  SyncRenewRequest$Outbound,
  SyncRenewRequest
> = z.object({
  workspace: z.string().optional(),
  syncRenewRequest: models.SyncRenewRequest$outboundSchema,
}).transform((v) => {
  return remap$(v, {
    syncRenewRequest: "SyncRenewRequest",
  });
});

export function syncRenewRequestToJSON(
  syncRenewRequest: SyncRenewRequest,
): string {
  return JSON.stringify(
    SyncRenewRequest$outboundSchema.parse(syncRenewRequest),
  );
}

/** @internal */
export const Lease$inboundSchema: z.ZodType<Lease, unknown> = z.object({
  deploymentId: z.string(),
  renewed: z.boolean(),
  leaseExpiresAt: z.nullable(
    z.iso.datetime({ offset: true }).transform(v => new Date(v)),
  ),
});

export function leaseFromJSON(
  jsonString: string,
): SafeParseResult<Lease, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Lease$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Lease' from JSON`,
  );
}

/** @internal */
export const SyncRenewResponse$inboundSchema: z.ZodType<
  SyncRenewResponse,
  unknown
> = z.object({
  success: z.boolean(),
  leases: z.array(z.lazy(() => Lease$inboundSchema)),
});

export function syncRenewResponseFromJSON(
  jsonString: string,
): SafeParseResult<SyncRenewResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SyncRenewResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SyncRenewResponse' from JSON`,
  );
}
