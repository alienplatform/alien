/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../../lib/primitives.js";
import * as models from "../index.js";

export type GenerateManagerTokenRequest = {
  /**
   * Unique identifier for a manager.
   */
  id: string;
  /**
   * Workspace name. Defaults to your last workspace (user auth) or your API key's workspace (token auth). When using an API key, if provided, must match the key's workspace.
   */
  workspace?: string | undefined;
  generateManagerTokenRequest?: models.GenerateManagerTokenRequest | undefined;
};

/** @internal */
export type GenerateManagerTokenRequest$Outbound = {
  id: string;
  workspace?: string | undefined;
  GenerateManagerTokenRequest?:
    | models.GenerateManagerTokenRequest$Outbound
    | undefined;
};

/** @internal */
export const GenerateManagerTokenRequest$outboundSchema: z.ZodType<
  GenerateManagerTokenRequest$Outbound,
  GenerateManagerTokenRequest
> = z.object({
  id: z.string(),
  workspace: z.string().optional(),
  generateManagerTokenRequest: models.GenerateManagerTokenRequest$outboundSchema
    .optional(),
}).transform((v) => {
  return remap$(v, {
    generateManagerTokenRequest: "GenerateManagerTokenRequest",
  });
});

export function generateManagerTokenRequestToJSON(
  generateManagerTokenRequest: GenerateManagerTokenRequest,
): string {
  return JSON.stringify(
    GenerateManagerTokenRequest$outboundSchema.parse(
      generateManagerTokenRequest,
    ),
  );
}
