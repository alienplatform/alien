/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import * as models from "../index.js";

export type ListReleaseChannelsRequest = {
  /**
   * Workspace name. Required for user/session/OAuth requests. Optional for API keys because API keys are workspace-scoped; if provided with an API key, it must match the key's workspace.
   */
  workspace?: string | undefined;
  /**
   * Filter by project ID or name.
   */
  project: string;
};

/**
 * Project release channels.
 */
export type ListReleaseChannelsResponse = {
  items: Array<models.ReleaseChannel>;
};

/** @internal */
export type ListReleaseChannelsRequest$Outbound = {
  workspace?: string | undefined;
  project: string;
};

/** @internal */
export const ListReleaseChannelsRequest$outboundSchema: z.ZodType<
  ListReleaseChannelsRequest$Outbound,
  ListReleaseChannelsRequest
> = z.object({
  workspace: z.string().optional(),
  project: z.string(),
});

export function listReleaseChannelsRequestToJSON(
  listReleaseChannelsRequest: ListReleaseChannelsRequest,
): string {
  return JSON.stringify(
    ListReleaseChannelsRequest$outboundSchema.parse(listReleaseChannelsRequest),
  );
}

/** @internal */
export const ListReleaseChannelsResponse$inboundSchema: z.ZodType<
  ListReleaseChannelsResponse,
  unknown
> = z.object({
  items: z.array(models.ReleaseChannel$inboundSchema),
});

export function listReleaseChannelsResponseFromJSON(
  jsonString: string,
): SafeParseResult<ListReleaseChannelsResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListReleaseChannelsResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListReleaseChannelsResponse' from JSON`,
  );
}
