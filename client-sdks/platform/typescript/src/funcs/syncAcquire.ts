/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import type { AlienCore } from "../core.js"
import { encodeFormQuery, encodeJSON } from "../lib/encodings.js"
import * as M from "../lib/matchers.js"
import { compactMap } from "../lib/primitives.js"
import { safeParse } from "../lib/schemas.js"
import type { RequestOptions } from "../lib/sdks.js"
import { extractSecurity, resolveGlobalSecurity } from "../lib/security.js"
import { pathToFunc } from "../lib/url.js"
import type { AlienError } from "../models/errors/alienerror.js"
import type {
  ConnectionError,
  InvalidRequestError,
  RequestAbortedError,
  RequestTimeoutError,
  UnexpectedClientError,
} from "../models/errors/httpclienterrors.js"
import * as errors from "../models/errors/index.js"
import type { ResponseValidationError } from "../models/errors/responsevalidationerror.js"
import type { SDKValidationError } from "../models/errors/sdkvalidationerror.js"
import * as models from "../models/index.js"
import * as operations from "../models/operations/index.js"
import { type APICall, APIPromise } from "../types/async.js"
import type { Result } from "../types/fp.js"

/**
 * Acquire a batch of deployments for processing. Used by Manager to atomically lock deployments matching filters. Each deployment in the batch must be released after processing.
 */
export function syncAcquire(
  client: AlienCore,
  request?: operations.SyncAcquireRequest | undefined,
  options?: RequestOptions,
): APIPromise<
  Result<
    models.SyncAcquireResponse,
    | errors.APIError
    | AlienError
    | ResponseValidationError
    | ConnectionError
    | RequestAbortedError
    | RequestTimeoutError
    | InvalidRequestError
    | UnexpectedClientError
    | SDKValidationError
  >
> {
  return new APIPromise($do(client, request, options))
}

async function $do(
  client: AlienCore,
  request?: operations.SyncAcquireRequest | undefined,
  options?: RequestOptions,
): Promise<
  [
    Result<
      models.SyncAcquireResponse,
      | errors.APIError
      | AlienError
      | ResponseValidationError
      | ConnectionError
      | RequestAbortedError
      | RequestTimeoutError
      | InvalidRequestError
      | UnexpectedClientError
      | SDKValidationError
    >,
    APICall,
  ]
> {
  const parsed = safeParse(
    request,
    value => operations.SyncAcquireRequest$outboundSchema.optional().parse(value),
    "Input validation failed",
  )
  if (!parsed.ok) {
    return [parsed, { status: "invalid" }]
  }
  const payload = parsed.value
  const body = encodeJSON("body", payload?.SyncAcquireRequest, {
    explode: true,
  })

  const path = pathToFunc("/v1/sync/acquire")()

  const query = encodeFormQuery({
    workspace: payload?.workspace,
  })

  const headers = new Headers(
    compactMap({
      "Content-Type": "application/json",
      Accept: "application/json",
    }),
  )

  const secConfig = await extractSecurity(client._options.apiKey)
  const securityInput = secConfig == null ? {} : { apiKey: secConfig }
  const requestSecurity = resolveGlobalSecurity(securityInput)

  const context = {
    options: client._options,
    baseURL: options?.serverURL ?? client._baseURL ?? "",
    operationID: "syncAcquire",
    oAuth2Scopes: null,

    resolvedSecurity: requestSecurity,

    securitySource: client._options.apiKey,
    retryConfig: options?.retries || client._options.retryConfig || { strategy: "none" },
    retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
  }

  const requestRes = client._createRequest(
    context,
    {
      security: requestSecurity,
      method: "POST",
      baseURL: options?.serverURL,
      path: path,
      headers: headers,
      query: query,
      body: body,
      userAgent: client._options.userAgent,
      timeoutMs: options?.timeoutMs || client._options.timeoutMs || -1,
    },
    options,
  )
  if (!requestRes.ok) {
    return [requestRes, { status: "invalid" }]
  }
  const req = requestRes.value

  const doResult = await client._do(req, {
    context,
    errorCodes: ["400", "4XX", "500", "5XX"],
    retryConfig: context.retryConfig,
    retryCodes: context.retryCodes,
  })
  if (!doResult.ok) {
    return [doResult, { status: "request-error", request: req }]
  }
  const response = doResult.value

  const responseFields = {
    HttpMeta: { Response: response, Request: req },
  }

  const [result] = await M.match<
    models.SyncAcquireResponse,
    | errors.APIError
    | AlienError
    | ResponseValidationError
    | ConnectionError
    | RequestAbortedError
    | RequestTimeoutError
    | InvalidRequestError
    | UnexpectedClientError
    | SDKValidationError
  >(
    M.json(200, models.SyncAcquireResponse$inboundSchema),
    M.jsonErr(400, errors.APIError$inboundSchema),
    M.jsonErr(500, errors.APIError$inboundSchema),
    M.fail("4XX"),
    M.fail("5XX"),
  )(response, req, { extraFields: responseFields })
  if (!result.ok) {
    return [result, { status: "complete", request: req, response }]
  }

  return [result, { status: "complete", request: req, response }]
}
