# SyncListResponseRegime

Supervisor regime reported on the last sync. Drives which `agent_target` payload (`binary` vs `helm`) the manager sends.

## Example Usage

```typescript
import { SyncListResponseRegime } from "@alienplatform/platform-api/models";

let value: SyncListResponseRegime = "os-service";
```

## Values

```typescript
"os-service" | "kubernetes"
```