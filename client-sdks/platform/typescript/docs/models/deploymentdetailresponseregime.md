# DeploymentDetailResponseRegime

Supervisor regime reported on the last sync. Drives which `agent_target` payload (`binary` vs `helm`) the manager sends.

## Example Usage

```typescript
import { DeploymentDetailResponseRegime } from "@alienplatform/platform-api/models";

let value: DeploymentDetailResponseRegime = "kubernetes";
```

## Values

```typescript
"os-service" | "kubernetes"
```