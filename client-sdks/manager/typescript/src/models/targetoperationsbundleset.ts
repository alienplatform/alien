/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";
import {
  OperationsBundleDownload,
  OperationsBundleDownload$inboundSchema,
} from "./operationsbundledownload.js";

/**
 * Target operations-bundle set for the Operator to converge its loaded
 *
 * @remarks
 * plugin registry toward, independent of any release/config target — a
 * plugin can be enabled with no release change, so this is not nested under
 * `TargetDeployment`.
 */
export type TargetOperationsBundleSet = {
  /**
   * Presigned downloads for every bundle in the target set. The Operator
   *
   * @remarks
   * fetches only the ones it doesn't already have loaded at the right
   * version; already-loaded bundles are harmless to re-download.
   */
  bundles?: Array<OperationsBundleDownload> | undefined;
  /**
   * Hash identifying this exact enabled-plugin set. Compare against
   *
   * @remarks
   * `OperationsReport.loadedBundleHash` to detect drift.
   */
  hash: string;
};

/** @internal */
export const TargetOperationsBundleSet$inboundSchema: z.ZodType<
  TargetOperationsBundleSet,
  unknown
> = z.object({
  bundles: z.array(OperationsBundleDownload$inboundSchema).optional(),
  hash: z.string(),
});

export function targetOperationsBundleSetFromJSON(
  jsonString: string,
): SafeParseResult<TargetOperationsBundleSet, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => TargetOperationsBundleSet$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TargetOperationsBundleSet' from JSON`,
  );
}
