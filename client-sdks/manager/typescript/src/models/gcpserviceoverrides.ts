/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

/**
 * Service endpoint overrides for testing GCP services
 */
export type GcpServiceOverrides = {
  /**
   * Override endpoints for specific GCP services
   *
   * @remarks
   * Key is the service name (e.g., "cloudrun", "storage"), value is the base URL
   */
  endpoints: { [k: string]: string };
};

/** @internal */
export const GcpServiceOverrides$inboundSchema: z.ZodType<
  GcpServiceOverrides,
  unknown
> = z.object({
  endpoints: z.record(z.string(), z.string()),
});

export function gcpServiceOverridesFromJSON(
  jsonString: string,
): SafeParseResult<GcpServiceOverrides, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GcpServiceOverrides$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GcpServiceOverrides' from JSON`,
  );
}
