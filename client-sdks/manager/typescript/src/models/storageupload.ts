/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";
import {
  PresignedRequest,
  PresignedRequest$inboundSchema,
} from "./presignedrequest.js";

/**
 * Storage upload information
 */
export type StorageUpload = {
  /**
   * Expiration time for upload URL
   */
  expiresAt: Date;
  /**
   * A presigned request that can be serialized, stored, and executed later.
   *
   * @remarks
   * Hides implementation details for different storage backends.
   */
  putRequest: PresignedRequest;
};

/** @internal */
export const StorageUpload$inboundSchema: z.ZodType<StorageUpload, unknown> = z
  .object({
    expiresAt: z.iso.datetime({ offset: true }).transform(v => new Date(v)),
    putRequest: PresignedRequest$inboundSchema,
  });

export function storageUploadFromJSON(
  jsonString: string,
): SafeParseResult<StorageUpload, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => StorageUpload$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'StorageUpload' from JSON`,
  );
}
