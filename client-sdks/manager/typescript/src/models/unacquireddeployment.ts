/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import {
  DeploymentAcquireUnavailableReason,
  DeploymentAcquireUnavailableReason$inboundSchema,
} from "./deploymentacquireunavailablereason.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

/**
 * Atomic outcome for one explicitly requested deployment that was not acquired.
 */
export type UnacquiredDeployment = {
  deploymentId: string;
  /**
   * Why an explicitly requested deployment was not acquired.
   *
   * @remarks
   *
   * These reasons are intentionally bounded and do not identify the session
   * that owns a competing lease.
   */
  reason: DeploymentAcquireUnavailableReason;
  retryAfter?: Date | null | undefined;
};

/** @internal */
export const UnacquiredDeployment$inboundSchema: z.ZodType<
  UnacquiredDeployment,
  unknown
> = z.object({
  deploymentId: z.string(),
  reason: DeploymentAcquireUnavailableReason$inboundSchema,
  retryAfter: z.nullable(
    z.iso.datetime({ offset: true }).transform(v => new Date(v)),
  ).optional(),
});

export function unacquiredDeploymentFromJSON(
  jsonString: string,
): SafeParseResult<UnacquiredDeployment, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => UnacquiredDeployment$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'UnacquiredDeployment' from JSON`,
  );
}
