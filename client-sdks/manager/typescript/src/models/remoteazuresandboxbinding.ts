/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

/**
 * Concrete sandbox-group topology returned to remote clients.
 *
 * @remarks
 *
 * The ceilings travel because Azure applies them at create and nowhere else, so a remote caller
 * that does not send them gets the data plane's default rather than the declared size. They are
 * not a limit — a holder that ignores them gets whatever the data plane accepts.
 */
export type RemoteAzureSandboxBinding = {
  /**
   * Whether the declaration asked for open egress. Always true here, sent explicitly because
   *
   * @remarks
   * the remote grant lets its holder create sandboxes the declared policy never reaches — a
   * client must read this rather than assume it from an absent field.
   */
  allowEgress: boolean;
  /**
   * Declared sandbox ceilings, where the declaration named them.
   */
  cpu?: string | null | undefined;
  /**
   * Per-region data-plane host, which is separate from the ARM control plane.
   */
  dataPlaneEndpoint: string;
  disk?: string | null | undefined;
  /**
   * Catalog disk image every sandbox is created from.
   */
  diskImage: string;
  /**
   * Idle seconds after which a sandbox pauses, where the declaration asked for one.
   */
  idlePauseSeconds?: number | null | undefined;
  memory?: string | null | undefined;
  /**
   * Region the sandbox group lives in.
   */
  region: string;
  /**
   * Resource group the data-plane path is scoped by.
   */
  resourceGroup: string;
  /**
   * Sandbox group the credential lease authorizes sandboxes against.
   */
  sandboxGroup: string;
};

/** @internal */
export const RemoteAzureSandboxBinding$inboundSchema: z.ZodType<
  RemoteAzureSandboxBinding,
  unknown
> = z.object({
  allowEgress: z.boolean(),
  cpu: z.nullable(z.string()).optional(),
  dataPlaneEndpoint: z.string(),
  disk: z.nullable(z.string()).optional(),
  diskImage: z.string(),
  idlePauseSeconds: z.nullable(z.int()).optional(),
  memory: z.nullable(z.string()).optional(),
  region: z.string(),
  resourceGroup: z.string(),
  sandboxGroup: z.string(),
});

export function remoteAzureSandboxBindingFromJSON(
  jsonString: string,
): SafeParseResult<RemoteAzureSandboxBinding, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => RemoteAzureSandboxBinding$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'RemoteAzureSandboxBinding' from JSON`,
  );
}
