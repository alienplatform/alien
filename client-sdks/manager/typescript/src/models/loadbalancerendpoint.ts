/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

/**
 * Load balancer endpoint information for DNS management.
 *
 * @remarks
 * This is optional metadata used by the DNS controller to create domain mappings.
 */
export type LoadBalancerEndpoint = {
  /**
   * The DNS name of the load balancer endpoint (e.g., ALB DNS, API Gateway domain).
   */
  dnsName: string;
  /**
   * AWS Route53 hosted zone ID (for ALIAS records). Only set on AWS.
   */
  hostedZoneId?: string | null | undefined;
};

/** @internal */
export const LoadBalancerEndpoint$inboundSchema: z.ZodType<
  LoadBalancerEndpoint,
  unknown
> = z.object({
  dnsName: z.string(),
  hostedZoneId: z.nullable(z.string()).optional(),
});

export function loadBalancerEndpointFromJSON(
  jsonString: string,
): SafeParseResult<LoadBalancerEndpoint, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => LoadBalancerEndpoint$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'LoadBalancerEndpoint' from JSON`,
  );
}
