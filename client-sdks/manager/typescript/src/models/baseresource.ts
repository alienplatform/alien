/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../lib/primitives.js";
import {
  collectExtraKeys as collectExtraKeys$,
  safeParse,
} from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

/**
 * Resource that can hold any resource type in the Alien system. All resources share common 'type' and 'id' fields with additional type-specific properties.
 */
export type BaseResource = {
  /**
   * The unique identifier for this specific resource instance. Must contain only alphanumeric characters, hyphens, and underscores ([A-Za-z0-9-_]). Maximum 64 characters.
   */
  id: string;
  /**
   * Resource type identifier that determines the specific kind of resource. This field is used for polymorphic deserialization and resource-specific behavior.
   */
  type: string;
  additionalProperties?: { [k: string]: any } | undefined;
};

/** @internal */
export const BaseResource$inboundSchema: z.ZodType<BaseResource, unknown> =
  collectExtraKeys$(
    z.object({
      id: z.string(),
      type: z.string(),
    }).catchall(z.any()),
    "additionalProperties",
    true,
  );
/** @internal */
export type BaseResource$Outbound = {
  id: string;
  type: string;
  [additionalProperties: string]: unknown;
};

/** @internal */
export const BaseResource$outboundSchema: z.ZodType<
  BaseResource$Outbound,
  BaseResource
> = z.object({
  id: z.string(),
  type: z.string(),
  additionalProperties: z.record(z.string(), z.any()).optional(),
}).transform((v) => {
  return {
    ...v.additionalProperties,
    ...remap$(v, {
      additionalProperties: null,
    }),
  };
});

export function baseResourceToJSON(baseResource: BaseResource): string {
  return JSON.stringify(BaseResource$outboundSchema.parse(baseResource));
}
export function baseResourceFromJSON(
  jsonString: string,
): SafeParseResult<BaseResource, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => BaseResource$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'BaseResource' from JSON`,
  );
}
