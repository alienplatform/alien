/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";
import {
  PresignedRequest,
  PresignedRequest$inboundSchema,
} from "./presignedrequest.js";

/**
 * Response handling configuration for deployments
 */
export type ResponseHandling = {
  /**
   * Maximum response body size that can be submitted inline
   */
  maxInlineBytes: number;
  /**
   * A presigned request that can be serialized, stored, and executed later.
   *
   * @remarks
   * Hides implementation details for different storage backends.
   */
  storageUploadRequest: PresignedRequest;
  /**
   * URL where deployments submit responses
   */
  submitResponseUrl: string;
};

/** @internal */
export const ResponseHandling$inboundSchema: z.ZodType<
  ResponseHandling,
  unknown
> = z.object({
  maxInlineBytes: z.int(),
  storageUploadRequest: PresignedRequest$inboundSchema,
  submitResponseUrl: z.string(),
});

export function responseHandlingFromJSON(
  jsonString: string,
): SafeParseResult<ResponseHandling, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ResponseHandling$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ResponseHandling' from JSON`,
  );
}
