/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

/**
 * Concrete Agent Platform topology returned to remote clients.
 *
 * @remarks
 *
 * No egress field, unlike the other two clouds: the policy lives on the environment template
 * named below, so it travels with the template rather than as a flag the client must read.
 */
export type RemoteGcpSandboxBinding = {
  /**
   * Reasoning engine the credential lease authorizes sandboxes under.
   */
  engine: string;
  /**
   * Seconds a sandbox may live, where the declaration asked for one.
   */
  maxLifetimeSeconds?: number | null | undefined;
  /**
   * Region selecting the regional aiplatform endpoint.
   */
  region: string;
  /**
   * Environment template every sandbox is created from; it carries the image and the ceilings.
   */
  template: string;
};

/** @internal */
export const RemoteGcpSandboxBinding$inboundSchema: z.ZodType<
  RemoteGcpSandboxBinding,
  unknown
> = z.object({
  engine: z.string(),
  maxLifetimeSeconds: z.nullable(z.int()).optional(),
  region: z.string(),
  template: z.string(),
});

export function remoteGcpSandboxBindingFromJSON(
  jsonString: string,
): SafeParseResult<RemoteGcpSandboxBinding, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => RemoteGcpSandboxBinding$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'RemoteGcpSandboxBinding' from JSON`,
  );
}
