/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4";
import {
  ObservedApplicationImage,
  ObservedApplicationImage$Outbound,
  ObservedApplicationImage$outboundSchema,
} from "./observedapplicationimage.js";
import {
  ObservedApplicationSource,
  ObservedApplicationSource$outboundSchema,
} from "./observedapplicationsource.js";

/**
 * Application release the Operator observes running in its environment.
 *
 * @remarks
 *
 * This identifies the customer's application, not the Operator: the
 * Operator's own image is reported separately as [`OperatorImageReport`].
 */
export type ObservedApplicationReport = {
  /**
   * Helm chart name from the workloads' `helm.sh/chart` label. Present only
   *
   * @remarks
   * when every labelled workload names the same chart.
   */
  chartName?: string | null | undefined;
  /**
   * Helm chart version from the same label.
   */
  chartVersion?: string | null | undefined;
  /**
   * Whether every workload kind could be listed. When `false`, the chart
   *
   * @remarks
   * and images describe only the workloads the Operator could read.
   */
  complete: boolean;
  /**
   * Distinct container images running in the observed workloads.
   */
  images?: Array<ObservedApplicationImage> | undefined;
  /**
   * When the workloads were read.
   */
  observedAt: Date;
  /**
   * Where the Operator read the application identity from.
   */
  source: ObservedApplicationSource;
};

/** @internal */
export type ObservedApplicationReport$Outbound = {
  chartName?: string | null | undefined;
  chartVersion?: string | null | undefined;
  complete: boolean;
  images?: Array<ObservedApplicationImage$Outbound> | undefined;
  observedAt: string;
  source: string;
};

/** @internal */
export const ObservedApplicationReport$outboundSchema: z.ZodType<
  ObservedApplicationReport$Outbound,
  ObservedApplicationReport
> = z.object({
  chartName: z.nullable(z.string()).optional(),
  chartVersion: z.nullable(z.string()).optional(),
  complete: z.boolean(),
  images: z.array(ObservedApplicationImage$outboundSchema).optional(),
  observedAt: z.date().transform(v => v.toISOString()),
  source: ObservedApplicationSource$outboundSchema,
});

export function observedApplicationReportToJSON(
  observedApplicationReport: ObservedApplicationReport,
): string {
  return JSON.stringify(
    ObservedApplicationReport$outboundSchema.parse(observedApplicationReport),
  );
}
