/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";
import {
  PresignedOperation,
  PresignedOperation$inboundSchema,
  PresignedOperation$outboundSchema,
} from "./presignedoperation.js";
import {
  PresignedRequestBackend,
  PresignedRequestBackend$inboundSchema,
  PresignedRequestBackend$Outbound,
  PresignedRequestBackend$outboundSchema,
} from "./presignedrequestbackend.js";

/**
 * A presigned request that can be serialized, stored, and executed later.
 *
 * @remarks
 * Hides implementation details for different storage backends.
 */
export type PresignedRequest = {
  /**
   * Storage backend representation for different presigned request types
   */
  backend: PresignedRequestBackend;
  /**
   * When this presigned request expires
   */
  expiration: Date;
  /**
   * The type of operation a presigned request performs
   */
  operation: PresignedOperation;
  /**
   * The path this request operates on
   */
  path: string;
};

/** @internal */
export const PresignedRequest$inboundSchema: z.ZodType<
  PresignedRequest,
  unknown
> = z.object({
  backend: PresignedRequestBackend$inboundSchema,
  expiration: z.iso.datetime({ offset: true }).transform(v => new Date(v)),
  operation: PresignedOperation$inboundSchema,
  path: z.string(),
});
/** @internal */
export type PresignedRequest$Outbound = {
  backend: PresignedRequestBackend$Outbound;
  expiration: string;
  operation: string;
  path: string;
};

/** @internal */
export const PresignedRequest$outboundSchema: z.ZodType<
  PresignedRequest$Outbound,
  PresignedRequest
> = z.object({
  backend: PresignedRequestBackend$outboundSchema,
  expiration: z.date().transform(v => v.toISOString()),
  operation: PresignedOperation$outboundSchema,
  path: z.string(),
});

export function presignedRequestToJSON(
  presignedRequest: PresignedRequest,
): string {
  return JSON.stringify(
    PresignedRequest$outboundSchema.parse(presignedRequest),
  );
}
export function presignedRequestFromJSON(
  jsonString: string,
): SafeParseResult<PresignedRequest, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PresignedRequest$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PresignedRequest' from JSON`,
  );
}
