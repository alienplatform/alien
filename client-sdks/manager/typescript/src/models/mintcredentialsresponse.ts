/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import {
  ClientConfigUnion,
  ClientConfigUnion$inboundSchema,
} from "./clientconfigunion.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

/**
 * Response body for `POST /v1/credentials/mint`.
 */
export type MintCredentialsResponse = {
  /**
   * Configuration for different cloud platform clients
   */
  clientConfig: ClientConfigUnion;
  /**
   * Credential expiry as an RFC3339 timestamp (now + clamped duration).
   *
   * @remarks
   *
   * This is server-computed (`now + clamped duration`), not read back from
   * the provider — for lazily-resolved configs (e.g. GCP, where the
   * resolver doesn't always round-trip an authoritative expiry) it is
   * nominal rather than provider truth. Treat it as a refresh hint: fetch
   * new credentials at or before this time, don't rely on it to prove the
   * underlying credential is still valid at that exact instant.
   */
  expiresAt: string;
  /**
   * Human-readable identity the credentials act as (role ARN, SA email,
   *
   * @remarks
   * managed-identity client id, or `platform:account` for the local path).
   */
  principal: string;
};

/** @internal */
export const MintCredentialsResponse$inboundSchema: z.ZodType<
  MintCredentialsResponse,
  unknown
> = z.object({
  clientConfig: ClientConfigUnion$inboundSchema,
  expiresAt: z.string(),
  principal: z.string(),
});

export function mintCredentialsResponseFromJSON(
  jsonString: string,
): SafeParseResult<MintCredentialsResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => MintCredentialsResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'MintCredentialsResponse' from JSON`,
  );
}
