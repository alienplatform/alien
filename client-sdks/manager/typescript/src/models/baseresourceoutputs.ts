/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../lib/primitives.js";
import {
  collectExtraKeys as collectExtraKeys$,
  safeParse,
} from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

/**
 * Resource outputs that can hold output data for any resource type in the Alien system. All resource outputs share a common 'type' field with additional type-specific output properties.
 */
export type BaseResourceOutputs = {
  /**
   * Resource type identifier that determines the specific kind of resource. This field is used for polymorphic deserialization and resource-specific behavior.
   */
  type: string;
  additionalProperties?: { [k: string]: any } | undefined;
};

/** @internal */
export const BaseResourceOutputs$inboundSchema: z.ZodType<
  BaseResourceOutputs,
  unknown
> = collectExtraKeys$(
  z.object({
    type: z.string(),
  }).catchall(z.any()),
  "additionalProperties",
  true,
);
/** @internal */
export type BaseResourceOutputs$Outbound = {
  type: string;
  [additionalProperties: string]: unknown;
};

/** @internal */
export const BaseResourceOutputs$outboundSchema: z.ZodType<
  BaseResourceOutputs$Outbound,
  BaseResourceOutputs
> = z.object({
  type: z.string(),
  additionalProperties: z.record(z.string(), z.any()).optional(),
}).transform((v) => {
  return {
    ...v.additionalProperties,
    ...remap$(v, {
      additionalProperties: null,
    }),
  };
});

export function baseResourceOutputsToJSON(
  baseResourceOutputs: BaseResourceOutputs,
): string {
  return JSON.stringify(
    BaseResourceOutputs$outboundSchema.parse(baseResourceOutputs),
  );
}
export function baseResourceOutputsFromJSON(
  jsonString: string,
): SafeParseResult<BaseResourceOutputs, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => BaseResourceOutputs$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'BaseResourceOutputs' from JSON`,
  );
}
