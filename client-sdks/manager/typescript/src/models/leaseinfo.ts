/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { Envelope, Envelope$inboundSchema } from "./envelope.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

/**
 * Lease information
 */
export type LeaseInfo = {
  /**
   * Attempt number
   */
  attempt: number;
  /**
   * Command identifier
   */
  commandId: string;
  /**
   * Commands envelope sent to deployments
   */
  envelope: Envelope;
  /**
   * When lease expires
   */
  leaseExpiresAt: Date;
  /**
   * Unique lease identifier
   */
  leaseId: string;
};

/** @internal */
export const LeaseInfo$inboundSchema: z.ZodType<LeaseInfo, unknown> = z.object({
  attempt: z.int(),
  commandId: z.string(),
  envelope: Envelope$inboundSchema,
  leaseExpiresAt: z.iso.datetime({ offset: true }).transform(v => new Date(v)),
  leaseId: z.string(),
});

export function leaseInfoFromJSON(
  jsonString: string,
): SafeParseResult<LeaseInfo, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => LeaseInfo$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'LeaseInfo' from JSON`,
  );
}
