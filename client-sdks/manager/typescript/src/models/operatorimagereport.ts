/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4";
import {
  OperatorImageSource,
  OperatorImageSource$outboundSchema,
} from "./operatorimagesource.js";

/**
 * Exact immutable Operator image identity observed by the running process.
 */
export type OperatorImageReport = {
  /**
   * Exact lowercase OCI digest in `sha256:digest` form.
   */
  digest: string;
  /**
   * Exact OCI image reference in `repository@sha256:digest` form.
   */
  image: string;
  /**
   * Package ID when `source` is `package`; otherwise `null`.
   */
  packageId?: string | null | undefined;
  /**
   * Package version when `source` is `package`; otherwise `null`.
   */
  packageVersion?: string | null | undefined;
  /**
   * Origin of the exact Operator image running this process.
   */
  source: OperatorImageSource;
};

/** @internal */
export type OperatorImageReport$Outbound = {
  digest: string;
  image: string;
  packageId?: string | null | undefined;
  packageVersion?: string | null | undefined;
  source: string;
};

/** @internal */
export const OperatorImageReport$outboundSchema: z.ZodType<
  OperatorImageReport$Outbound,
  OperatorImageReport
> = z.object({
  digest: z.string(),
  image: z.string(),
  packageId: z.nullable(z.string()).optional(),
  packageVersion: z.nullable(z.string()).optional(),
  source: OperatorImageSource$outboundSchema,
});

export function operatorImageReportToJSON(
  operatorImageReport: OperatorImageReport,
): string {
  return JSON.stringify(
    OperatorImageReport$outboundSchema.parse(operatorImageReport),
  );
}
