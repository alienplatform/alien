/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

/**
 * One bundle the Operator needs to download to reach `targetBundleHash`.
 *
 * @remarks
 * The manager mints a short-lived presigned GET URL per bundle — the
 * Operator never holds real cloud storage credentials, mirroring the OCI
 * registry proxy's credential-injection pattern.
 */
export type OperationsBundleDownload = {
  /**
   * Plugin name this bundle provides.
   */
  plugin: string;
  /**
   * Plugin version this bundle provides.
   */
  pluginVersion: string;
  /**
   * Presigned URL to GET the bundle ZIP from. Short-lived.
   */
  url: string;
};

/** @internal */
export const OperationsBundleDownload$inboundSchema: z.ZodType<
  OperationsBundleDownload,
  unknown
> = z.object({
  plugin: z.string(),
  pluginVersion: z.string(),
  url: z.string(),
});

export function operationsBundleDownloadFromJSON(
  jsonString: string,
): SafeParseResult<OperationsBundleDownload, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => OperationsBundleDownload$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'OperationsBundleDownload' from JSON`,
  );
}
