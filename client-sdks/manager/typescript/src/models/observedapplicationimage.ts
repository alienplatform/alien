/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4";

/**
 * A container image running in one observed application workload.
 */
export type ObservedApplicationImage = {
  /**
   * Container name within the workload.
   */
  container: string;
  /**
   * Registry manifest digest in `sha256:<hex>` form, when the runtime
   *
   * @remarks
   * reports one.
   */
  digest?: string | null | undefined;
  /**
   * Image reference reported by the container runtime.
   */
  image: string;
  /**
   * Inventory identity of the workload, matching the `rawIdentity` of its
   *
   * @remarks
   * observed resource sample (for example `apps/v1:Deployment:shop:api`).
   */
  workload: string;
};

/** @internal */
export type ObservedApplicationImage$Outbound = {
  container: string;
  digest?: string | null | undefined;
  image: string;
  workload: string;
};

/** @internal */
export const ObservedApplicationImage$outboundSchema: z.ZodType<
  ObservedApplicationImage$Outbound,
  ObservedApplicationImage
> = z.object({
  container: z.string(),
  digest: z.nullable(z.string()).optional(),
  image: z.string(),
  workload: z.string(),
});

export function observedApplicationImageToJSON(
  observedApplicationImage: ObservedApplicationImage,
): string {
  return JSON.stringify(
    ObservedApplicationImage$outboundSchema.parse(observedApplicationImage),
  );
}
