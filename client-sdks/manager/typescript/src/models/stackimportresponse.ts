/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";
import { StackSettings, StackSettings$inboundSchema } from "./stacksettings.js";
import { StackState, StackState$inboundSchema } from "./stackstate.js";

/**
 * Response body returned after a stack import.
 */
export type StackImportResponse = {
  /**
   * Deployment created.
   */
  deploymentId: string;
  /**
   * User-customizable deployment settings specified at deploy time.
   *
   * @remarks
   *
   * These settings are provided by the customer via CloudFormation parameters,
   * Terraform attributes, CLI flags, or Helm values. They customize how the
   * deployment runs and what capabilities are enabled.
   *
   * **Key distinction**: StackSettings is user-customizable, while ManagementConfig
   * is platform-derived (from the Manager's ServiceAccount).
   */
  stackSettings: StackSettings;
  /**
   * Represents the collective state of all resources in a stack, including platform and pending actions.
   */
  stackState: StackState;
};

/** @internal */
export const StackImportResponse$inboundSchema: z.ZodType<
  StackImportResponse,
  unknown
> = z.object({
  deploymentId: z.string(),
  stackSettings: StackSettings$inboundSchema,
  stackState: StackState$inboundSchema,
});

export function stackImportResponseFromJSON(
  jsonString: string,
): SafeParseResult<StackImportResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => StackImportResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'StackImportResponse' from JSON`,
  );
}
