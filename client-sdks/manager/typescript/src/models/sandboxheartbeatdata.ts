/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4";
import {
  SandboxHeartbeatStatus,
  SandboxHeartbeatStatus$Outbound,
  SandboxHeartbeatStatus$outboundSchema,
} from "./sandboxheartbeatstatus.js";

/**
 * Local: containers Docker still holds for this sandbox.
 */
export type SandboxHeartbeatDataLocal = {
  activeSessions: number;
  /**
   * Whether the loopback route is serving in this process. False after a manager restart until
   *
   * @remarks
   * the next tick rebinds it.
   */
  routeServing: boolean;
  status: SandboxHeartbeatStatus;
  backend: "local";
};

/**
 * Kubernetes: pods carrying the sandbox label, in the deployment's namespace.
 */
export type SandboxHeartbeatDataKubernetesPods = {
  activeSessions: number;
  /**
   * Claimed but unused pods waiting in the pool.
   */
  idlePods: number;
  namespace: string;
  status: SandboxHeartbeatStatus;
  backend: "kubernetesPods";
};

/**
 * GCP: the Agent Platform template sessions are cut from, and the engine it hangs under.
 *
 * @remarks
 *
 * No session count: that needs `aiplatform.sandboxEnvironments.list`, which only the management
 * permission set holds. No template state either — emission is gated on reading it `ACTIVE`,
 * which is what `status` already says.
 */
export type SandboxHeartbeatDataGcpAgentPlatform = {
  /**
   * Reasoning engine the template hangs under, without which the template id names nothing.
   */
  engine: string;
  status: SandboxHeartbeatStatus;
  /**
   * The template sessions are currently cut from.
   */
  templateId: string;
  backend: "gcpAgentPlatform";
};

/**
 * Azure: the sandbox group's ARM state. The data plane has no list operation, so a session count
 *
 * @remarks
 * is not available here.
 */
export type SandboxHeartbeatDataAzureSandboxGroup = {
  provisioningState?: string | null | undefined;
  sandboxGroup: string;
  status: SandboxHeartbeatStatus;
  backend: "azureSandboxGroup";
};

/**
 * AWS: the image a sandbox runs from, and the lifecycle state AWS reports for it.
 */
export type SandboxHeartbeatDataAwsMicrovm = {
  /**
   * Image the sessions belong to.
   */
  imageIdentifier: string;
  /**
   * The image's own lifecycle state, which is where AWS surfaces base-image deprecation.
   *
   * @remarks
   *
   * No session count sits beside it: counting means `lambda:ListMicrovms`, which authorizes
   * against no resource type and so cannot be granted without an account-wide reach the
   * permission sets refuse. A field only an over-broad grant could fill is a field whose
   * implementer ships AccessDenied into a customer's account.
   */
  imageState?: string | null | undefined;
  status: SandboxHeartbeatStatus;
  backend: "awsMicrovm";
};

/**
 * Content-free telemetry about a sandbox's sessions.
 *
 * @remarks
 *
 * Never anything from inside a session. A controller reaches only the cloud's management APIs,
 * and the whole point of the resource is that the control plane cannot see what runs in it.
 */
export type SandboxHeartbeatData =
  | SandboxHeartbeatDataAwsMicrovm
  | SandboxHeartbeatDataAzureSandboxGroup
  | SandboxHeartbeatDataGcpAgentPlatform
  | SandboxHeartbeatDataKubernetesPods
  | SandboxHeartbeatDataLocal;

/** @internal */
export type SandboxHeartbeatDataLocal$Outbound = {
  activeSessions: number;
  routeServing: boolean;
  status: SandboxHeartbeatStatus$Outbound;
  backend: "local";
};

/** @internal */
export const SandboxHeartbeatDataLocal$outboundSchema: z.ZodType<
  SandboxHeartbeatDataLocal$Outbound,
  SandboxHeartbeatDataLocal
> = z.object({
  activeSessions: z.int(),
  routeServing: z.boolean(),
  status: SandboxHeartbeatStatus$outboundSchema,
  backend: z.literal("local"),
});

export function sandboxHeartbeatDataLocalToJSON(
  sandboxHeartbeatDataLocal: SandboxHeartbeatDataLocal,
): string {
  return JSON.stringify(
    SandboxHeartbeatDataLocal$outboundSchema.parse(sandboxHeartbeatDataLocal),
  );
}

/** @internal */
export type SandboxHeartbeatDataKubernetesPods$Outbound = {
  activeSessions: number;
  idlePods: number;
  namespace: string;
  status: SandboxHeartbeatStatus$Outbound;
  backend: "kubernetesPods";
};

/** @internal */
export const SandboxHeartbeatDataKubernetesPods$outboundSchema: z.ZodType<
  SandboxHeartbeatDataKubernetesPods$Outbound,
  SandboxHeartbeatDataKubernetesPods
> = z.object({
  activeSessions: z.int(),
  idlePods: z.int(),
  namespace: z.string(),
  status: SandboxHeartbeatStatus$outboundSchema,
  backend: z.literal("kubernetesPods"),
});

export function sandboxHeartbeatDataKubernetesPodsToJSON(
  sandboxHeartbeatDataKubernetesPods: SandboxHeartbeatDataKubernetesPods,
): string {
  return JSON.stringify(
    SandboxHeartbeatDataKubernetesPods$outboundSchema.parse(
      sandboxHeartbeatDataKubernetesPods,
    ),
  );
}

/** @internal */
export type SandboxHeartbeatDataGcpAgentPlatform$Outbound = {
  engine: string;
  status: SandboxHeartbeatStatus$Outbound;
  templateId: string;
  backend: "gcpAgentPlatform";
};

/** @internal */
export const SandboxHeartbeatDataGcpAgentPlatform$outboundSchema: z.ZodType<
  SandboxHeartbeatDataGcpAgentPlatform$Outbound,
  SandboxHeartbeatDataGcpAgentPlatform
> = z.object({
  engine: z.string(),
  status: SandboxHeartbeatStatus$outboundSchema,
  templateId: z.string(),
  backend: z.literal("gcpAgentPlatform"),
});

export function sandboxHeartbeatDataGcpAgentPlatformToJSON(
  sandboxHeartbeatDataGcpAgentPlatform: SandboxHeartbeatDataGcpAgentPlatform,
): string {
  return JSON.stringify(
    SandboxHeartbeatDataGcpAgentPlatform$outboundSchema.parse(
      sandboxHeartbeatDataGcpAgentPlatform,
    ),
  );
}

/** @internal */
export type SandboxHeartbeatDataAzureSandboxGroup$Outbound = {
  provisioningState?: string | null | undefined;
  sandboxGroup: string;
  status: SandboxHeartbeatStatus$Outbound;
  backend: "azureSandboxGroup";
};

/** @internal */
export const SandboxHeartbeatDataAzureSandboxGroup$outboundSchema: z.ZodType<
  SandboxHeartbeatDataAzureSandboxGroup$Outbound,
  SandboxHeartbeatDataAzureSandboxGroup
> = z.object({
  provisioningState: z.nullable(z.string()).optional(),
  sandboxGroup: z.string(),
  status: SandboxHeartbeatStatus$outboundSchema,
  backend: z.literal("azureSandboxGroup"),
});

export function sandboxHeartbeatDataAzureSandboxGroupToJSON(
  sandboxHeartbeatDataAzureSandboxGroup: SandboxHeartbeatDataAzureSandboxGroup,
): string {
  return JSON.stringify(
    SandboxHeartbeatDataAzureSandboxGroup$outboundSchema.parse(
      sandboxHeartbeatDataAzureSandboxGroup,
    ),
  );
}

/** @internal */
export type SandboxHeartbeatDataAwsMicrovm$Outbound = {
  imageIdentifier: string;
  imageState?: string | null | undefined;
  status: SandboxHeartbeatStatus$Outbound;
  backend: "awsMicrovm";
};

/** @internal */
export const SandboxHeartbeatDataAwsMicrovm$outboundSchema: z.ZodType<
  SandboxHeartbeatDataAwsMicrovm$Outbound,
  SandboxHeartbeatDataAwsMicrovm
> = z.object({
  imageIdentifier: z.string(),
  imageState: z.nullable(z.string()).optional(),
  status: SandboxHeartbeatStatus$outboundSchema,
  backend: z.literal("awsMicrovm"),
});

export function sandboxHeartbeatDataAwsMicrovmToJSON(
  sandboxHeartbeatDataAwsMicrovm: SandboxHeartbeatDataAwsMicrovm,
): string {
  return JSON.stringify(
    SandboxHeartbeatDataAwsMicrovm$outboundSchema.parse(
      sandboxHeartbeatDataAwsMicrovm,
    ),
  );
}

/** @internal */
export type SandboxHeartbeatData$Outbound =
  | SandboxHeartbeatDataAwsMicrovm$Outbound
  | SandboxHeartbeatDataAzureSandboxGroup$Outbound
  | SandboxHeartbeatDataGcpAgentPlatform$Outbound
  | SandboxHeartbeatDataKubernetesPods$Outbound
  | SandboxHeartbeatDataLocal$Outbound;

/** @internal */
export const SandboxHeartbeatData$outboundSchema: z.ZodType<
  SandboxHeartbeatData$Outbound,
  SandboxHeartbeatData
> = z.union([
  z.lazy(() => SandboxHeartbeatDataAwsMicrovm$outboundSchema),
  z.lazy(() => SandboxHeartbeatDataAzureSandboxGroup$outboundSchema),
  z.lazy(() => SandboxHeartbeatDataGcpAgentPlatform$outboundSchema),
  z.lazy(() => SandboxHeartbeatDataKubernetesPods$outboundSchema),
  z.lazy(() => SandboxHeartbeatDataLocal$outboundSchema),
]);

export function sandboxHeartbeatDataToJSON(
  sandboxHeartbeatData: SandboxHeartbeatData,
): string {
  return JSON.stringify(
    SandboxHeartbeatData$outboundSchema.parse(sandboxHeartbeatData),
  );
}
