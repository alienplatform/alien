/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

/**
 * W3C Trace Context propagated with a command lease.
 *
 * @remarks
 *
 * The values are kept in their standard wire form so receivers can attach
 * them to handler telemetry without inventing separate trace/span fields.
 */
export type TraceContext = {
  /**
   * W3C `traceparent` header value.
   */
  traceparent: string;
  /**
   * Optional W3C `tracestate` header value.
   */
  tracestate?: string | null | undefined;
};

/** @internal */
export const TraceContext$inboundSchema: z.ZodType<TraceContext, unknown> = z
  .object({
    traceparent: z.string(),
    tracestate: z.nullable(z.string()).optional(),
  });

export function traceContextFromJSON(
  jsonString: string,
): SafeParseResult<TraceContext, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => TraceContext$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TraceContext' from JSON`,
  );
}
