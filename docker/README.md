# Container Images

Canonical Dockerfiles for OSS image builds:

- `Dockerfile.alien-base`
- `Dockerfile.alien-builder`

Images are built and published by GitHub Actions workflows in `.github/workflows/images*.yml`.
