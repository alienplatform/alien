// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.10.1
//   protoc               v7.34.1
// source: queue.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import type { CallContext, CallOptions } from "nice-grpc-common";

export const protobufPackage = "alien_bindings.queue";

/** Messages for Send */
export interface SendRequest {
  /** Name of the Queue binding to use */
  bindingName: string;
  /** Queue name to send to */
  queue: string;
  /** Message payload */
  message: MessagePayload | undefined;
}

/** Empty response on success */
export interface SendResponse {
}

/** Messages for Receive */
export interface ReceiveRequest {
  /** Name of the Queue binding to use */
  bindingName: string;
  /** Queue name to receive from */
  queue: string;
  /** Maximum number of messages to receive (1-10) */
  maxMessages: number;
}

export interface ReceiveResponse {
  /** List of received messages */
  messages: QueueMessage[];
}

/** Messages for Ack */
export interface AckRequest {
  /** Name of the Queue binding to use */
  bindingName: string;
  /** Queue name */
  queue: string;
  /** Receipt handle for acknowledgment */
  receiptHandle: string;
}

/** Empty response on success */
export interface AckResponse {
}

/** Message payload types */
export interface MessagePayload {
  /** JSON-serializable value (as JSON string) */
  json?:
    | string
    | undefined;
  /** UTF-8 text payload */
  text?: string | undefined;
}

/** Queue message with receipt handle */
export interface QueueMessage {
  /** Message payload */
  payload:
    | MessagePayload
    | undefined;
  /** Opaque receipt handle for acknowledgment */
  receiptHandle: string;
}

function createBaseSendRequest(): SendRequest {
  return { bindingName: "", queue: "", message: undefined };
}

export const SendRequest: MessageFns<SendRequest> = {
  encode(message: SendRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.bindingName !== "") {
      writer.uint32(10).string(message.bindingName);
    }
    if (message.queue !== "") {
      writer.uint32(18).string(message.queue);
    }
    if (message.message !== undefined) {
      MessagePayload.encode(message.message, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SendRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSendRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.bindingName = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.queue = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.message = MessagePayload.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SendRequest {
    return {
      bindingName: isSet(object.bindingName) ? globalThis.String(object.bindingName) : "",
      queue: isSet(object.queue) ? globalThis.String(object.queue) : "",
      message: isSet(object.message) ? MessagePayload.fromJSON(object.message) : undefined,
    };
  },

  toJSON(message: SendRequest): unknown {
    const obj: any = {};
    if (message.bindingName !== "") {
      obj.bindingName = message.bindingName;
    }
    if (message.queue !== "") {
      obj.queue = message.queue;
    }
    if (message.message !== undefined) {
      obj.message = MessagePayload.toJSON(message.message);
    }
    return obj;
  },

  create(base?: DeepPartial<SendRequest>): SendRequest {
    return SendRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SendRequest>): SendRequest {
    const message = createBaseSendRequest();
    message.bindingName = object.bindingName ?? "";
    message.queue = object.queue ?? "";
    message.message = (object.message !== undefined && object.message !== null)
      ? MessagePayload.fromPartial(object.message)
      : undefined;
    return message;
  },
};

function createBaseSendResponse(): SendResponse {
  return {};
}

export const SendResponse: MessageFns<SendResponse> = {
  encode(_: SendResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SendResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSendResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): SendResponse {
    return {};
  },

  toJSON(_: SendResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<SendResponse>): SendResponse {
    return SendResponse.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<SendResponse>): SendResponse {
    const message = createBaseSendResponse();
    return message;
  },
};

function createBaseReceiveRequest(): ReceiveRequest {
  return { bindingName: "", queue: "", maxMessages: 0 };
}

export const ReceiveRequest: MessageFns<ReceiveRequest> = {
  encode(message: ReceiveRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.bindingName !== "") {
      writer.uint32(10).string(message.bindingName);
    }
    if (message.queue !== "") {
      writer.uint32(18).string(message.queue);
    }
    if (message.maxMessages !== 0) {
      writer.uint32(24).uint32(message.maxMessages);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ReceiveRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReceiveRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.bindingName = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.queue = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.maxMessages = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ReceiveRequest {
    return {
      bindingName: isSet(object.bindingName) ? globalThis.String(object.bindingName) : "",
      queue: isSet(object.queue) ? globalThis.String(object.queue) : "",
      maxMessages: isSet(object.maxMessages) ? globalThis.Number(object.maxMessages) : 0,
    };
  },

  toJSON(message: ReceiveRequest): unknown {
    const obj: any = {};
    if (message.bindingName !== "") {
      obj.bindingName = message.bindingName;
    }
    if (message.queue !== "") {
      obj.queue = message.queue;
    }
    if (message.maxMessages !== 0) {
      obj.maxMessages = Math.round(message.maxMessages);
    }
    return obj;
  },

  create(base?: DeepPartial<ReceiveRequest>): ReceiveRequest {
    return ReceiveRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ReceiveRequest>): ReceiveRequest {
    const message = createBaseReceiveRequest();
    message.bindingName = object.bindingName ?? "";
    message.queue = object.queue ?? "";
    message.maxMessages = object.maxMessages ?? 0;
    return message;
  },
};

function createBaseReceiveResponse(): ReceiveResponse {
  return { messages: [] };
}

export const ReceiveResponse: MessageFns<ReceiveResponse> = {
  encode(message: ReceiveResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.messages) {
      QueueMessage.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ReceiveResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReceiveResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.messages.push(QueueMessage.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ReceiveResponse {
    return {
      messages: globalThis.Array.isArray(object?.messages)
        ? object.messages.map((e: any) => QueueMessage.fromJSON(e))
        : [],
    };
  },

  toJSON(message: ReceiveResponse): unknown {
    const obj: any = {};
    if (message.messages?.length) {
      obj.messages = message.messages.map((e) => QueueMessage.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<ReceiveResponse>): ReceiveResponse {
    return ReceiveResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ReceiveResponse>): ReceiveResponse {
    const message = createBaseReceiveResponse();
    message.messages = object.messages?.map((e) => QueueMessage.fromPartial(e)) || [];
    return message;
  },
};

function createBaseAckRequest(): AckRequest {
  return { bindingName: "", queue: "", receiptHandle: "" };
}

export const AckRequest: MessageFns<AckRequest> = {
  encode(message: AckRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.bindingName !== "") {
      writer.uint32(10).string(message.bindingName);
    }
    if (message.queue !== "") {
      writer.uint32(18).string(message.queue);
    }
    if (message.receiptHandle !== "") {
      writer.uint32(26).string(message.receiptHandle);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AckRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAckRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.bindingName = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.queue = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.receiptHandle = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AckRequest {
    return {
      bindingName: isSet(object.bindingName) ? globalThis.String(object.bindingName) : "",
      queue: isSet(object.queue) ? globalThis.String(object.queue) : "",
      receiptHandle: isSet(object.receiptHandle) ? globalThis.String(object.receiptHandle) : "",
    };
  },

  toJSON(message: AckRequest): unknown {
    const obj: any = {};
    if (message.bindingName !== "") {
      obj.bindingName = message.bindingName;
    }
    if (message.queue !== "") {
      obj.queue = message.queue;
    }
    if (message.receiptHandle !== "") {
      obj.receiptHandle = message.receiptHandle;
    }
    return obj;
  },

  create(base?: DeepPartial<AckRequest>): AckRequest {
    return AckRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<AckRequest>): AckRequest {
    const message = createBaseAckRequest();
    message.bindingName = object.bindingName ?? "";
    message.queue = object.queue ?? "";
    message.receiptHandle = object.receiptHandle ?? "";
    return message;
  },
};

function createBaseAckResponse(): AckResponse {
  return {};
}

export const AckResponse: MessageFns<AckResponse> = {
  encode(_: AckResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AckResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAckResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): AckResponse {
    return {};
  },

  toJSON(_: AckResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<AckResponse>): AckResponse {
    return AckResponse.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<AckResponse>): AckResponse {
    const message = createBaseAckResponse();
    return message;
  },
};

function createBaseMessagePayload(): MessagePayload {
  return { json: undefined, text: undefined };
}

export const MessagePayload: MessageFns<MessagePayload> = {
  encode(message: MessagePayload, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.json !== undefined) {
      writer.uint32(10).string(message.json);
    }
    if (message.text !== undefined) {
      writer.uint32(18).string(message.text);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MessagePayload {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMessagePayload();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.json = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.text = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MessagePayload {
    return {
      json: isSet(object.json) ? globalThis.String(object.json) : undefined,
      text: isSet(object.text) ? globalThis.String(object.text) : undefined,
    };
  },

  toJSON(message: MessagePayload): unknown {
    const obj: any = {};
    if (message.json !== undefined) {
      obj.json = message.json;
    }
    if (message.text !== undefined) {
      obj.text = message.text;
    }
    return obj;
  },

  create(base?: DeepPartial<MessagePayload>): MessagePayload {
    return MessagePayload.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MessagePayload>): MessagePayload {
    const message = createBaseMessagePayload();
    message.json = object.json ?? undefined;
    message.text = object.text ?? undefined;
    return message;
  },
};

function createBaseQueueMessage(): QueueMessage {
  return { payload: undefined, receiptHandle: "" };
}

export const QueueMessage: MessageFns<QueueMessage> = {
  encode(message: QueueMessage, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.payload !== undefined) {
      MessagePayload.encode(message.payload, writer.uint32(10).fork()).join();
    }
    if (message.receiptHandle !== "") {
      writer.uint32(18).string(message.receiptHandle);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueueMessage {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueueMessage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.payload = MessagePayload.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.receiptHandle = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueueMessage {
    return {
      payload: isSet(object.payload) ? MessagePayload.fromJSON(object.payload) : undefined,
      receiptHandle: isSet(object.receiptHandle) ? globalThis.String(object.receiptHandle) : "",
    };
  },

  toJSON(message: QueueMessage): unknown {
    const obj: any = {};
    if (message.payload !== undefined) {
      obj.payload = MessagePayload.toJSON(message.payload);
    }
    if (message.receiptHandle !== "") {
      obj.receiptHandle = message.receiptHandle;
    }
    return obj;
  },

  create(base?: DeepPartial<QueueMessage>): QueueMessage {
    return QueueMessage.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueueMessage>): QueueMessage {
    const message = createBaseQueueMessage();
    message.payload = (object.payload !== undefined && object.payload !== null)
      ? MessagePayload.fromPartial(object.payload)
      : undefined;
    message.receiptHandle = object.receiptHandle ?? "";
    return message;
  },
};

/** Queue service for message queue operations */
export type QueueServiceDefinition = typeof QueueServiceDefinition;
export const QueueServiceDefinition = {
  name: "QueueService",
  fullName: "alien_bindings.queue.QueueService",
  methods: {
    /** Send a message to the queue */
    send: {
      name: "Send",
      requestType: SendRequest,
      requestStream: false,
      responseType: SendResponse,
      responseStream: false,
      options: {},
    },
    /** Receive messages from the queue */
    receive: {
      name: "Receive",
      requestType: ReceiveRequest,
      requestStream: false,
      responseType: ReceiveResponse,
      responseStream: false,
      options: {},
    },
    /** Acknowledge processing of a message (permanently removes from queue) */
    ack: {
      name: "Ack",
      requestType: AckRequest,
      requestStream: false,
      responseType: AckResponse,
      responseStream: false,
      options: {},
    },
  },
} as const;

export interface QueueServiceImplementation<CallContextExt = {}> {
  /** Send a message to the queue */
  send(request: SendRequest, context: CallContext & CallContextExt): Promise<DeepPartial<SendResponse>>;
  /** Receive messages from the queue */
  receive(request: ReceiveRequest, context: CallContext & CallContextExt): Promise<DeepPartial<ReceiveResponse>>;
  /** Acknowledge processing of a message (permanently removes from queue) */
  ack(request: AckRequest, context: CallContext & CallContextExt): Promise<DeepPartial<AckResponse>>;
}

export interface QueueServiceClient<CallOptionsExt = {}> {
  /** Send a message to the queue */
  send(request: DeepPartial<SendRequest>, options?: CallOptions & CallOptionsExt): Promise<SendResponse>;
  /** Receive messages from the queue */
  receive(request: DeepPartial<ReceiveRequest>, options?: CallOptions & CallOptionsExt): Promise<ReceiveResponse>;
  /** Acknowledge processing of a message (permanently removes from queue) */
  ack(request: DeepPartial<AckRequest>, options?: CallOptions & CallOptionsExt): Promise<AckResponse>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
