// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.10.1
//   protoc               v5.29.3
// source: control.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import type { CallContext, CallOptions } from "nice-grpc-common";
import { Timestamp } from "./google/protobuf/timestamp.js";

export const protobufPackage = "alien_bindings.control";

export interface RegisterHttpServerRequest {
  /** The port the application's HTTP server is listening on */
  port: number;
}

export interface RegisterHttpServerResponse {
  success: boolean;
}

export interface RegisterEventHandlerRequest {
  /** Handler type: "storage", "cron", "queue", "command" */
  handlerType: string;
  /** Resource name: bucket name, schedule name, queue name, or command name */
  resourceName: string;
}

export interface RegisterEventHandlerResponse {
  success: boolean;
}

export interface WaitForTasksRequest {
  /** Unique identifier for the application instance */
  applicationId: string;
}

/** Task delivered to the application */
export interface Task {
  /** Unique task ID for correlation */
  taskId: string;
  storageEvent?: StorageEvent | undefined;
  cronEvent?: CronEvent | undefined;
  queueMessage?: QueueMessage | undefined;
  arcCommand?: ArcCommand | undefined;
}

export interface StorageEvent {
  /** The object key that triggered the event */
  key: string;
  /** Event type: "created", "deleted", "modified" */
  eventType: string;
  /** Bucket/container name */
  bucket: string;
  /** Object size in bytes (for created/modified) */
  size: number;
  /** Content type (for created/modified) */
  contentType: string;
  /** Event timestamp */
  timestamp: Date | undefined;
}

export interface CronEvent {
  /** Name of the schedule that triggered */
  scheduleName: string;
  /** When it was scheduled to run */
  scheduledTime: Date | undefined;
}

export interface QueueMessage {
  /** Message ID */
  id: string;
  /** Message payload (JSON bytes) */
  payload: Uint8Array;
  /** Receipt handle for acknowledgment */
  receiptHandle: string;
  /** Source queue name */
  source: string;
  /** Delivery attempt count */
  attemptCount: number;
  /** Message timestamp */
  timestamp: Date | undefined;
}

export interface ArcCommand {
  /** Unique command ID */
  commandId: string;
  /** Command name (e.g., "generate-report") */
  commandName: string;
  /** Command parameters (JSON bytes) */
  params: Uint8Array;
  /** Attempt number (starts at 1) */
  attempt: number;
  /** Deadline for completion (RFC3339) */
  deadline:
    | Date
    | undefined;
  /** URL to submit response to */
  responseUrl: string;
  /** URL to upload large response bodies */
  storageUploadUrl: string;
  /** Maximum inline response size */
  maxInlineBytes: number;
}

export interface SendTaskResultRequest {
  /** The task ID this result is for */
  taskId: string;
  success?: TaskSuccess | undefined;
  error?: TaskError | undefined;
}

export interface TaskSuccess {
  /** Response data (JSON bytes, for ARC commands) */
  responseData: Uint8Array;
}

export interface TaskError {
  /** Error code */
  code: string;
  /** Error message */
  message: string;
}

export interface SendTaskResultResponse {
  acknowledged: boolean;
}

function createBaseRegisterHttpServerRequest(): RegisterHttpServerRequest {
  return { port: 0 };
}

export const RegisterHttpServerRequest: MessageFns<RegisterHttpServerRequest> = {
  encode(message: RegisterHttpServerRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.port !== 0) {
      writer.uint32(8).uint32(message.port);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RegisterHttpServerRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRegisterHttpServerRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.port = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RegisterHttpServerRequest {
    return { port: isSet(object.port) ? globalThis.Number(object.port) : 0 };
  },

  toJSON(message: RegisterHttpServerRequest): unknown {
    const obj: any = {};
    if (message.port !== 0) {
      obj.port = Math.round(message.port);
    }
    return obj;
  },

  create(base?: DeepPartial<RegisterHttpServerRequest>): RegisterHttpServerRequest {
    return RegisterHttpServerRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<RegisterHttpServerRequest>): RegisterHttpServerRequest {
    const message = createBaseRegisterHttpServerRequest();
    message.port = object.port ?? 0;
    return message;
  },
};

function createBaseRegisterHttpServerResponse(): RegisterHttpServerResponse {
  return { success: false };
}

export const RegisterHttpServerResponse: MessageFns<RegisterHttpServerResponse> = {
  encode(message: RegisterHttpServerResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.success !== false) {
      writer.uint32(8).bool(message.success);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RegisterHttpServerResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRegisterHttpServerResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RegisterHttpServerResponse {
    return { success: isSet(object.success) ? globalThis.Boolean(object.success) : false };
  },

  toJSON(message: RegisterHttpServerResponse): unknown {
    const obj: any = {};
    if (message.success !== false) {
      obj.success = message.success;
    }
    return obj;
  },

  create(base?: DeepPartial<RegisterHttpServerResponse>): RegisterHttpServerResponse {
    return RegisterHttpServerResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<RegisterHttpServerResponse>): RegisterHttpServerResponse {
    const message = createBaseRegisterHttpServerResponse();
    message.success = object.success ?? false;
    return message;
  },
};

function createBaseRegisterEventHandlerRequest(): RegisterEventHandlerRequest {
  return { handlerType: "", resourceName: "" };
}

export const RegisterEventHandlerRequest: MessageFns<RegisterEventHandlerRequest> = {
  encode(message: RegisterEventHandlerRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.handlerType !== "") {
      writer.uint32(10).string(message.handlerType);
    }
    if (message.resourceName !== "") {
      writer.uint32(18).string(message.resourceName);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RegisterEventHandlerRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRegisterEventHandlerRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.handlerType = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.resourceName = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RegisterEventHandlerRequest {
    return {
      handlerType: isSet(object.handlerType) ? globalThis.String(object.handlerType) : "",
      resourceName: isSet(object.resourceName) ? globalThis.String(object.resourceName) : "",
    };
  },

  toJSON(message: RegisterEventHandlerRequest): unknown {
    const obj: any = {};
    if (message.handlerType !== "") {
      obj.handlerType = message.handlerType;
    }
    if (message.resourceName !== "") {
      obj.resourceName = message.resourceName;
    }
    return obj;
  },

  create(base?: DeepPartial<RegisterEventHandlerRequest>): RegisterEventHandlerRequest {
    return RegisterEventHandlerRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<RegisterEventHandlerRequest>): RegisterEventHandlerRequest {
    const message = createBaseRegisterEventHandlerRequest();
    message.handlerType = object.handlerType ?? "";
    message.resourceName = object.resourceName ?? "";
    return message;
  },
};

function createBaseRegisterEventHandlerResponse(): RegisterEventHandlerResponse {
  return { success: false };
}

export const RegisterEventHandlerResponse: MessageFns<RegisterEventHandlerResponse> = {
  encode(message: RegisterEventHandlerResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.success !== false) {
      writer.uint32(8).bool(message.success);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RegisterEventHandlerResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRegisterEventHandlerResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RegisterEventHandlerResponse {
    return { success: isSet(object.success) ? globalThis.Boolean(object.success) : false };
  },

  toJSON(message: RegisterEventHandlerResponse): unknown {
    const obj: any = {};
    if (message.success !== false) {
      obj.success = message.success;
    }
    return obj;
  },

  create(base?: DeepPartial<RegisterEventHandlerResponse>): RegisterEventHandlerResponse {
    return RegisterEventHandlerResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<RegisterEventHandlerResponse>): RegisterEventHandlerResponse {
    const message = createBaseRegisterEventHandlerResponse();
    message.success = object.success ?? false;
    return message;
  },
};

function createBaseWaitForTasksRequest(): WaitForTasksRequest {
  return { applicationId: "" };
}

export const WaitForTasksRequest: MessageFns<WaitForTasksRequest> = {
  encode(message: WaitForTasksRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.applicationId !== "") {
      writer.uint32(10).string(message.applicationId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WaitForTasksRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWaitForTasksRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.applicationId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WaitForTasksRequest {
    return { applicationId: isSet(object.applicationId) ? globalThis.String(object.applicationId) : "" };
  },

  toJSON(message: WaitForTasksRequest): unknown {
    const obj: any = {};
    if (message.applicationId !== "") {
      obj.applicationId = message.applicationId;
    }
    return obj;
  },

  create(base?: DeepPartial<WaitForTasksRequest>): WaitForTasksRequest {
    return WaitForTasksRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<WaitForTasksRequest>): WaitForTasksRequest {
    const message = createBaseWaitForTasksRequest();
    message.applicationId = object.applicationId ?? "";
    return message;
  },
};

function createBaseTask(): Task {
  return { taskId: "", storageEvent: undefined, cronEvent: undefined, queueMessage: undefined, arcCommand: undefined };
}

export const Task: MessageFns<Task> = {
  encode(message: Task, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.taskId !== "") {
      writer.uint32(10).string(message.taskId);
    }
    if (message.storageEvent !== undefined) {
      StorageEvent.encode(message.storageEvent, writer.uint32(18).fork()).join();
    }
    if (message.cronEvent !== undefined) {
      CronEvent.encode(message.cronEvent, writer.uint32(26).fork()).join();
    }
    if (message.queueMessage !== undefined) {
      QueueMessage.encode(message.queueMessage, writer.uint32(34).fork()).join();
    }
    if (message.arcCommand !== undefined) {
      ArcCommand.encode(message.arcCommand, writer.uint32(42).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Task {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTask();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.taskId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.storageEvent = StorageEvent.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.cronEvent = CronEvent.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.queueMessage = QueueMessage.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.arcCommand = ArcCommand.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Task {
    return {
      taskId: isSet(object.taskId) ? globalThis.String(object.taskId) : "",
      storageEvent: isSet(object.storageEvent) ? StorageEvent.fromJSON(object.storageEvent) : undefined,
      cronEvent: isSet(object.cronEvent) ? CronEvent.fromJSON(object.cronEvent) : undefined,
      queueMessage: isSet(object.queueMessage) ? QueueMessage.fromJSON(object.queueMessage) : undefined,
      arcCommand: isSet(object.arcCommand) ? ArcCommand.fromJSON(object.arcCommand) : undefined,
    };
  },

  toJSON(message: Task): unknown {
    const obj: any = {};
    if (message.taskId !== "") {
      obj.taskId = message.taskId;
    }
    if (message.storageEvent !== undefined) {
      obj.storageEvent = StorageEvent.toJSON(message.storageEvent);
    }
    if (message.cronEvent !== undefined) {
      obj.cronEvent = CronEvent.toJSON(message.cronEvent);
    }
    if (message.queueMessage !== undefined) {
      obj.queueMessage = QueueMessage.toJSON(message.queueMessage);
    }
    if (message.arcCommand !== undefined) {
      obj.arcCommand = ArcCommand.toJSON(message.arcCommand);
    }
    return obj;
  },

  create(base?: DeepPartial<Task>): Task {
    return Task.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Task>): Task {
    const message = createBaseTask();
    message.taskId = object.taskId ?? "";
    message.storageEvent = (object.storageEvent !== undefined && object.storageEvent !== null)
      ? StorageEvent.fromPartial(object.storageEvent)
      : undefined;
    message.cronEvent = (object.cronEvent !== undefined && object.cronEvent !== null)
      ? CronEvent.fromPartial(object.cronEvent)
      : undefined;
    message.queueMessage = (object.queueMessage !== undefined && object.queueMessage !== null)
      ? QueueMessage.fromPartial(object.queueMessage)
      : undefined;
    message.arcCommand = (object.arcCommand !== undefined && object.arcCommand !== null)
      ? ArcCommand.fromPartial(object.arcCommand)
      : undefined;
    return message;
  },
};

function createBaseStorageEvent(): StorageEvent {
  return { key: "", eventType: "", bucket: "", size: 0, contentType: "", timestamp: undefined };
}

export const StorageEvent: MessageFns<StorageEvent> = {
  encode(message: StorageEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.eventType !== "") {
      writer.uint32(18).string(message.eventType);
    }
    if (message.bucket !== "") {
      writer.uint32(26).string(message.bucket);
    }
    if (message.size !== 0) {
      writer.uint32(32).uint64(message.size);
    }
    if (message.contentType !== "") {
      writer.uint32(42).string(message.contentType);
    }
    if (message.timestamp !== undefined) {
      Timestamp.encode(toTimestamp(message.timestamp), writer.uint32(50).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StorageEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStorageEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.eventType = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.bucket = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.size = longToNumber(reader.uint64());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.contentType = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.timestamp = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StorageEvent {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      eventType: isSet(object.eventType) ? globalThis.String(object.eventType) : "",
      bucket: isSet(object.bucket) ? globalThis.String(object.bucket) : "",
      size: isSet(object.size) ? globalThis.Number(object.size) : 0,
      contentType: isSet(object.contentType) ? globalThis.String(object.contentType) : "",
      timestamp: isSet(object.timestamp) ? fromJsonTimestamp(object.timestamp) : undefined,
    };
  },

  toJSON(message: StorageEvent): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.eventType !== "") {
      obj.eventType = message.eventType;
    }
    if (message.bucket !== "") {
      obj.bucket = message.bucket;
    }
    if (message.size !== 0) {
      obj.size = Math.round(message.size);
    }
    if (message.contentType !== "") {
      obj.contentType = message.contentType;
    }
    if (message.timestamp !== undefined) {
      obj.timestamp = message.timestamp.toISOString();
    }
    return obj;
  },

  create(base?: DeepPartial<StorageEvent>): StorageEvent {
    return StorageEvent.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<StorageEvent>): StorageEvent {
    const message = createBaseStorageEvent();
    message.key = object.key ?? "";
    message.eventType = object.eventType ?? "";
    message.bucket = object.bucket ?? "";
    message.size = object.size ?? 0;
    message.contentType = object.contentType ?? "";
    message.timestamp = object.timestamp ?? undefined;
    return message;
  },
};

function createBaseCronEvent(): CronEvent {
  return { scheduleName: "", scheduledTime: undefined };
}

export const CronEvent: MessageFns<CronEvent> = {
  encode(message: CronEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.scheduleName !== "") {
      writer.uint32(10).string(message.scheduleName);
    }
    if (message.scheduledTime !== undefined) {
      Timestamp.encode(toTimestamp(message.scheduledTime), writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CronEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCronEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.scheduleName = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.scheduledTime = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CronEvent {
    return {
      scheduleName: isSet(object.scheduleName) ? globalThis.String(object.scheduleName) : "",
      scheduledTime: isSet(object.scheduledTime) ? fromJsonTimestamp(object.scheduledTime) : undefined,
    };
  },

  toJSON(message: CronEvent): unknown {
    const obj: any = {};
    if (message.scheduleName !== "") {
      obj.scheduleName = message.scheduleName;
    }
    if (message.scheduledTime !== undefined) {
      obj.scheduledTime = message.scheduledTime.toISOString();
    }
    return obj;
  },

  create(base?: DeepPartial<CronEvent>): CronEvent {
    return CronEvent.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CronEvent>): CronEvent {
    const message = createBaseCronEvent();
    message.scheduleName = object.scheduleName ?? "";
    message.scheduledTime = object.scheduledTime ?? undefined;
    return message;
  },
};

function createBaseQueueMessage(): QueueMessage {
  return { id: "", payload: new Uint8Array(0), receiptHandle: "", source: "", attemptCount: 0, timestamp: undefined };
}

export const QueueMessage: MessageFns<QueueMessage> = {
  encode(message: QueueMessage, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.payload.length !== 0) {
      writer.uint32(18).bytes(message.payload);
    }
    if (message.receiptHandle !== "") {
      writer.uint32(26).string(message.receiptHandle);
    }
    if (message.source !== "") {
      writer.uint32(34).string(message.source);
    }
    if (message.attemptCount !== 0) {
      writer.uint32(40).uint32(message.attemptCount);
    }
    if (message.timestamp !== undefined) {
      Timestamp.encode(toTimestamp(message.timestamp), writer.uint32(50).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueueMessage {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueueMessage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.payload = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.receiptHandle = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.source = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.attemptCount = reader.uint32();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.timestamp = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueueMessage {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      payload: isSet(object.payload) ? bytesFromBase64(object.payload) : new Uint8Array(0),
      receiptHandle: isSet(object.receiptHandle) ? globalThis.String(object.receiptHandle) : "",
      source: isSet(object.source) ? globalThis.String(object.source) : "",
      attemptCount: isSet(object.attemptCount) ? globalThis.Number(object.attemptCount) : 0,
      timestamp: isSet(object.timestamp) ? fromJsonTimestamp(object.timestamp) : undefined,
    };
  },

  toJSON(message: QueueMessage): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.payload.length !== 0) {
      obj.payload = base64FromBytes(message.payload);
    }
    if (message.receiptHandle !== "") {
      obj.receiptHandle = message.receiptHandle;
    }
    if (message.source !== "") {
      obj.source = message.source;
    }
    if (message.attemptCount !== 0) {
      obj.attemptCount = Math.round(message.attemptCount);
    }
    if (message.timestamp !== undefined) {
      obj.timestamp = message.timestamp.toISOString();
    }
    return obj;
  },

  create(base?: DeepPartial<QueueMessage>): QueueMessage {
    return QueueMessage.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueueMessage>): QueueMessage {
    const message = createBaseQueueMessage();
    message.id = object.id ?? "";
    message.payload = object.payload ?? new Uint8Array(0);
    message.receiptHandle = object.receiptHandle ?? "";
    message.source = object.source ?? "";
    message.attemptCount = object.attemptCount ?? 0;
    message.timestamp = object.timestamp ?? undefined;
    return message;
  },
};

function createBaseArcCommand(): ArcCommand {
  return {
    commandId: "",
    commandName: "",
    params: new Uint8Array(0),
    attempt: 0,
    deadline: undefined,
    responseUrl: "",
    storageUploadUrl: "",
    maxInlineBytes: 0,
  };
}

export const ArcCommand: MessageFns<ArcCommand> = {
  encode(message: ArcCommand, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.commandId !== "") {
      writer.uint32(10).string(message.commandId);
    }
    if (message.commandName !== "") {
      writer.uint32(18).string(message.commandName);
    }
    if (message.params.length !== 0) {
      writer.uint32(26).bytes(message.params);
    }
    if (message.attempt !== 0) {
      writer.uint32(32).uint32(message.attempt);
    }
    if (message.deadline !== undefined) {
      Timestamp.encode(toTimestamp(message.deadline), writer.uint32(42).fork()).join();
    }
    if (message.responseUrl !== "") {
      writer.uint32(50).string(message.responseUrl);
    }
    if (message.storageUploadUrl !== "") {
      writer.uint32(58).string(message.storageUploadUrl);
    }
    if (message.maxInlineBytes !== 0) {
      writer.uint32(64).uint64(message.maxInlineBytes);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ArcCommand {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseArcCommand();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.commandId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.commandName = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.params = reader.bytes();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.attempt = reader.uint32();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.deadline = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.responseUrl = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.storageUploadUrl = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.maxInlineBytes = longToNumber(reader.uint64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ArcCommand {
    return {
      commandId: isSet(object.commandId) ? globalThis.String(object.commandId) : "",
      commandName: isSet(object.commandName) ? globalThis.String(object.commandName) : "",
      params: isSet(object.params) ? bytesFromBase64(object.params) : new Uint8Array(0),
      attempt: isSet(object.attempt) ? globalThis.Number(object.attempt) : 0,
      deadline: isSet(object.deadline) ? fromJsonTimestamp(object.deadline) : undefined,
      responseUrl: isSet(object.responseUrl) ? globalThis.String(object.responseUrl) : "",
      storageUploadUrl: isSet(object.storageUploadUrl) ? globalThis.String(object.storageUploadUrl) : "",
      maxInlineBytes: isSet(object.maxInlineBytes) ? globalThis.Number(object.maxInlineBytes) : 0,
    };
  },

  toJSON(message: ArcCommand): unknown {
    const obj: any = {};
    if (message.commandId !== "") {
      obj.commandId = message.commandId;
    }
    if (message.commandName !== "") {
      obj.commandName = message.commandName;
    }
    if (message.params.length !== 0) {
      obj.params = base64FromBytes(message.params);
    }
    if (message.attempt !== 0) {
      obj.attempt = Math.round(message.attempt);
    }
    if (message.deadline !== undefined) {
      obj.deadline = message.deadline.toISOString();
    }
    if (message.responseUrl !== "") {
      obj.responseUrl = message.responseUrl;
    }
    if (message.storageUploadUrl !== "") {
      obj.storageUploadUrl = message.storageUploadUrl;
    }
    if (message.maxInlineBytes !== 0) {
      obj.maxInlineBytes = Math.round(message.maxInlineBytes);
    }
    return obj;
  },

  create(base?: DeepPartial<ArcCommand>): ArcCommand {
    return ArcCommand.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ArcCommand>): ArcCommand {
    const message = createBaseArcCommand();
    message.commandId = object.commandId ?? "";
    message.commandName = object.commandName ?? "";
    message.params = object.params ?? new Uint8Array(0);
    message.attempt = object.attempt ?? 0;
    message.deadline = object.deadline ?? undefined;
    message.responseUrl = object.responseUrl ?? "";
    message.storageUploadUrl = object.storageUploadUrl ?? "";
    message.maxInlineBytes = object.maxInlineBytes ?? 0;
    return message;
  },
};

function createBaseSendTaskResultRequest(): SendTaskResultRequest {
  return { taskId: "", success: undefined, error: undefined };
}

export const SendTaskResultRequest: MessageFns<SendTaskResultRequest> = {
  encode(message: SendTaskResultRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.taskId !== "") {
      writer.uint32(10).string(message.taskId);
    }
    if (message.success !== undefined) {
      TaskSuccess.encode(message.success, writer.uint32(18).fork()).join();
    }
    if (message.error !== undefined) {
      TaskError.encode(message.error, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SendTaskResultRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSendTaskResultRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.taskId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.success = TaskSuccess.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.error = TaskError.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SendTaskResultRequest {
    return {
      taskId: isSet(object.taskId) ? globalThis.String(object.taskId) : "",
      success: isSet(object.success) ? TaskSuccess.fromJSON(object.success) : undefined,
      error: isSet(object.error) ? TaskError.fromJSON(object.error) : undefined,
    };
  },

  toJSON(message: SendTaskResultRequest): unknown {
    const obj: any = {};
    if (message.taskId !== "") {
      obj.taskId = message.taskId;
    }
    if (message.success !== undefined) {
      obj.success = TaskSuccess.toJSON(message.success);
    }
    if (message.error !== undefined) {
      obj.error = TaskError.toJSON(message.error);
    }
    return obj;
  },

  create(base?: DeepPartial<SendTaskResultRequest>): SendTaskResultRequest {
    return SendTaskResultRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SendTaskResultRequest>): SendTaskResultRequest {
    const message = createBaseSendTaskResultRequest();
    message.taskId = object.taskId ?? "";
    message.success = (object.success !== undefined && object.success !== null)
      ? TaskSuccess.fromPartial(object.success)
      : undefined;
    message.error = (object.error !== undefined && object.error !== null)
      ? TaskError.fromPartial(object.error)
      : undefined;
    return message;
  },
};

function createBaseTaskSuccess(): TaskSuccess {
  return { responseData: new Uint8Array(0) };
}

export const TaskSuccess: MessageFns<TaskSuccess> = {
  encode(message: TaskSuccess, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.responseData.length !== 0) {
      writer.uint32(10).bytes(message.responseData);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TaskSuccess {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTaskSuccess();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.responseData = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TaskSuccess {
    return { responseData: isSet(object.responseData) ? bytesFromBase64(object.responseData) : new Uint8Array(0) };
  },

  toJSON(message: TaskSuccess): unknown {
    const obj: any = {};
    if (message.responseData.length !== 0) {
      obj.responseData = base64FromBytes(message.responseData);
    }
    return obj;
  },

  create(base?: DeepPartial<TaskSuccess>): TaskSuccess {
    return TaskSuccess.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<TaskSuccess>): TaskSuccess {
    const message = createBaseTaskSuccess();
    message.responseData = object.responseData ?? new Uint8Array(0);
    return message;
  },
};

function createBaseTaskError(): TaskError {
  return { code: "", message: "" };
}

export const TaskError: MessageFns<TaskError> = {
  encode(message: TaskError, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.code !== "") {
      writer.uint32(10).string(message.code);
    }
    if (message.message !== "") {
      writer.uint32(18).string(message.message);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TaskError {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTaskError();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.code = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.message = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TaskError {
    return {
      code: isSet(object.code) ? globalThis.String(object.code) : "",
      message: isSet(object.message) ? globalThis.String(object.message) : "",
    };
  },

  toJSON(message: TaskError): unknown {
    const obj: any = {};
    if (message.code !== "") {
      obj.code = message.code;
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    return obj;
  },

  create(base?: DeepPartial<TaskError>): TaskError {
    return TaskError.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<TaskError>): TaskError {
    const message = createBaseTaskError();
    message.code = object.code ?? "";
    message.message = object.message ?? "";
    return message;
  },
};

function createBaseSendTaskResultResponse(): SendTaskResultResponse {
  return { acknowledged: false };
}

export const SendTaskResultResponse: MessageFns<SendTaskResultResponse> = {
  encode(message: SendTaskResultResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.acknowledged !== false) {
      writer.uint32(8).bool(message.acknowledged);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SendTaskResultResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSendTaskResultResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.acknowledged = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SendTaskResultResponse {
    return { acknowledged: isSet(object.acknowledged) ? globalThis.Boolean(object.acknowledged) : false };
  },

  toJSON(message: SendTaskResultResponse): unknown {
    const obj: any = {};
    if (message.acknowledged !== false) {
      obj.acknowledged = message.acknowledged;
    }
    return obj;
  },

  create(base?: DeepPartial<SendTaskResultResponse>): SendTaskResultResponse {
    return SendTaskResultResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SendTaskResultResponse>): SendTaskResultResponse {
    const message = createBaseSendTaskResultResponse();
    message.acknowledged = object.acknowledged ?? false;
    return message;
  },
};

/**
 * Service for runtime-application control communication.
 * The runtime hosts this service, and applications connect to it.
 */
export type ControlServiceDefinition = typeof ControlServiceDefinition;
export const ControlServiceDefinition = {
  name: "ControlService",
  fullName: "alien_bindings.control.ControlService",
  methods: {
    /** App registers that it has an HTTP server listening on a port */
    registerHttpServer: {
      name: "RegisterHttpServer",
      requestType: RegisterHttpServerRequest,
      requestStream: false,
      responseType: RegisterHttpServerResponse,
      responseStream: false,
      options: {},
    },
    /** App registers an event handler for a specific resource */
    registerEventHandler: {
      name: "RegisterEventHandler",
      requestType: RegisterEventHandlerRequest,
      requestStream: false,
      responseType: RegisterEventHandlerResponse,
      responseStream: false,
      options: {},
    },
    /**
     * App waits for tasks - this is a server-streaming RPC
     * Runtime sends tasks as they arrive, app processes them
     */
    waitForTasks: {
      name: "WaitForTasks",
      requestType: WaitForTasksRequest,
      requestStream: false,
      responseType: Task,
      responseStream: true,
      options: {},
    },
    /** App sends result for a processed task */
    sendTaskResult: {
      name: "SendTaskResult",
      requestType: SendTaskResultRequest,
      requestStream: false,
      responseType: SendTaskResultResponse,
      responseStream: false,
      options: {},
    },
  },
} as const;

export interface ControlServiceImplementation<CallContextExt = {}> {
  /** App registers that it has an HTTP server listening on a port */
  registerHttpServer(
    request: RegisterHttpServerRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<RegisterHttpServerResponse>>;
  /** App registers an event handler for a specific resource */
  registerEventHandler(
    request: RegisterEventHandlerRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<RegisterEventHandlerResponse>>;
  /**
   * App waits for tasks - this is a server-streaming RPC
   * Runtime sends tasks as they arrive, app processes them
   */
  waitForTasks(
    request: WaitForTasksRequest,
    context: CallContext & CallContextExt,
  ): ServerStreamingMethodResult<DeepPartial<Task>>;
  /** App sends result for a processed task */
  sendTaskResult(
    request: SendTaskResultRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<SendTaskResultResponse>>;
}

export interface ControlServiceClient<CallOptionsExt = {}> {
  /** App registers that it has an HTTP server listening on a port */
  registerHttpServer(
    request: DeepPartial<RegisterHttpServerRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<RegisterHttpServerResponse>;
  /** App registers an event handler for a specific resource */
  registerEventHandler(
    request: DeepPartial<RegisterEventHandlerRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<RegisterEventHandlerResponse>;
  /**
   * App waits for tasks - this is a server-streaming RPC
   * Runtime sends tasks as they arrive, app processes them
   */
  waitForTasks(request: DeepPartial<WaitForTasksRequest>, options?: CallOptions & CallOptionsExt): AsyncIterable<Task>;
  /** App sends result for a processed task */
  sendTaskResult(
    request: DeepPartial<SendTaskResultRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<SendTaskResultResponse>;
}

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return globalThis.Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export type ServerStreamingMethodResult<Response> = { [Symbol.asyncIterator](): AsyncIterator<Response, void> };

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
