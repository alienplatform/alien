{
  "type": "object",
  "description": "GCP Storage ImportData — a Cloud Storage bucket created in the\ncustomer's project.",
  "required": ["bucketName", "bucketSelfLink", "projectId", "location"],
  "properties": {
    "bucketName": {
      "type": "string",
      "description": "Cloud Storage bucket name."
    },
    "bucketSelfLink": { "type": "string", "description": "Bucket self-link." },
    "location": {
      "type": "string",
      "description": "Bucket location (region or multi-region code)."
    },
    "projectId": {
      "type": "string",
      "description": "Project ID containing the bucket."
    }
  },
  "x-readme-ref-name": "GcpStorageImportData"
}
