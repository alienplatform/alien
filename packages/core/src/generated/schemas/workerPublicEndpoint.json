{
  "type": "object",
  "description": "Public endpoint configuration for Worker resources.",
  "required": ["name"],
  "properties": {
    "hostLabel": {
      "type": ["string", "null"],
      "description": "Optional DNS label override for generated endpoint hostnames."
    },
    "name": {
      "type": "string",
      "description": "Endpoint name within the resource."
    },
    "wildcardSubdomains": {
      "type": "boolean",
      "description": "Whether to route wildcard subdomains to this endpoint."
    }
  },
  "x-readme-ref-name": "WorkerPublicEndpoint"
}
