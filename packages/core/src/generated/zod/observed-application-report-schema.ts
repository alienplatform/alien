/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import * as z from "zod";
import { ObservedApplicationImageSchema } from "./observed-application-image-schema.js";
import { ObservedApplicationSourceSchema } from "./observed-application-source-schema.js";

/**
 * @description Application release the Operator observes running in its environment.\n\nThis identifies the customer\'s application, not the Operator: the\nOperator\'s own image is reported separately as [`OperatorImageReport`].
 */
export const ObservedApplicationReportSchema = z.object({
    "chartName": z.string().describe("Helm chart name from the workloads' `helm.sh/chart` label. Present only\nwhen every labelled workload names the same chart.").nullish(),
"chartVersion": z.string().describe("Helm chart version from the same label.").nullish(),
"complete": z.boolean().describe("Whether every workload kind could be listed. When `false`, the chart\nand images describe only the workloads the Operator could read."),
get "images"(){
                return z.array(ObservedApplicationImageSchema.describe("A container image running in one observed application workload.")).describe("Distinct container images running in the observed workloads.").optional()
              },
"observedAt": z.iso.datetime().describe("When the workloads were read."),
get "source"(){
                return ObservedApplicationSourceSchema.describe("Where the Operator read the application identity from.")
              }
    }).describe("Application release the Operator observes running in its environment.\n\nThis identifies the customer's application, not the Operator: the\nOperator's own image is reported separately as [`OperatorImageReport`].")

export type ObservedApplicationReport = z.infer<typeof ObservedApplicationReportSchema>