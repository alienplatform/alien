/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import * as z from "zod";
import { SandboxHeartbeatStatusSchema } from "./sandbox-heartbeat-status-schema.js";

/**
 * @description GCP: the Agent Platform template sessions are cut from, and the engine it hangs under.\n\nNo session count: that needs `aiplatform.sandboxEnvironments.list`, which only the management\npermission set holds. No template state either — emission is gated on reading it `ACTIVE`,\nwhich is what `status` already says.
 */
export const GcpAgentPlatformSandboxHeartbeatDataSchema = z.object({
    "engine": z.string().describe("Reasoning engine the template hangs under, without which the template id names nothing."),
get "status"(){
                return SandboxHeartbeatStatusSchema
              },
"templateId": z.string().describe("The template sessions are currently cut from.")
    }).describe("GCP: the Agent Platform template sessions are cut from, and the engine it hangs under.\n\nNo session count: that needs `aiplatform.sandboxEnvironments.list`, which only the management\npermission set holds. No template state either — emission is gated on reading it `ACTIVE`,\nwhich is what `status` already says.")

export type GcpAgentPlatformSandboxHeartbeatData = z.infer<typeof GcpAgentPlatformSandboxHeartbeatDataSchema>