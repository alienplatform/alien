/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import * as z from "zod";

/**
 * @description GCP Agent Platform reasoning-engine ImportData.\n\nEmitted only for a Frozen engine, which the setup stack creates. Vertex assigns the id, so it\nis read off the created resource rather than derived: the controller and the template beneath\nit address the engine by exactly this segment.
 */
export const GcpAgentPlatformEngineImportDataSchema = z.object({
    "engineId": z.string().describe("The engine's resource name, or the bare id it ends with.")
    }).describe("GCP Agent Platform reasoning-engine ImportData.\n\nEmitted only for a Frozen engine, which the setup stack creates. Vertex assigns the id, so it\nis read off the created resource rather than derived: the controller and the template beneath\nit address the engine by exactly this segment.")

export type GcpAgentPlatformEngineImportData = z.infer<typeof GcpAgentPlatformEngineImportDataSchema>