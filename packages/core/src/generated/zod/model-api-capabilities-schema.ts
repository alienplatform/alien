/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import * as z from "zod";
import { ClientApiSchema } from "./client-api-schema.js";
import { ModelCapabilitySupportSchema } from "./model-capability-support-schema.js";

/**
 * @description Qualified model behavior for one public Gateway API.
 */
export const ModelApiCapabilitiesSchema = z.object({
    get "api"(){
                return ClientApiSchema.describe("A public API accepted from an application client.")
              },
get "functionTools"(){
                return ModelCapabilitySupportSchema.describe("Whether a model feature has been qualified through one public Gateway API.\n\n`Unverified` is deliberately different from `Unsupported`: Alien does not infer support from\na model family or from the provider-native protocol used behind the Gateway.")
              },
get "imageInput"(){
                return ModelCapabilitySupportSchema.describe("Whether a model feature has been qualified through one public Gateway API.\n\n`Unverified` is deliberately different from `Unsupported`: Alien does not infer support from\na model family or from the provider-native protocol used behind the Gateway.")
              },
"qualifiedOn": z.string().describe("Date of the live qualification evidence, or `None` when every fact is unverified.").nullish(),
get "reasoningControls"(){
                return ModelCapabilitySupportSchema.describe("Whether a model feature has been qualified through one public Gateway API.\n\n`Unverified` is deliberately different from `Unsupported`: Alien does not infer support from\na model family or from the provider-native protocol used behind the Gateway.")
              },
get "serverManagedContinuation"(){
                return ModelCapabilitySupportSchema.describe("Whether a model feature has been qualified through one public Gateway API.\n\n`Unverified` is deliberately different from `Unsupported`: Alien does not infer support from\na model family or from the provider-native protocol used behind the Gateway.")
              },
get "statelessReplay"(){
                return ModelCapabilitySupportSchema.describe("Whether a model feature has been qualified through one public Gateway API.\n\n`Unverified` is deliberately different from `Unsupported`: Alien does not infer support from\na model family or from the provider-native protocol used behind the Gateway.")
              }
    }).describe("Qualified model behavior for one public Gateway API.")

export type ModelApiCapabilities = z.infer<typeof ModelApiCapabilitiesSchema>