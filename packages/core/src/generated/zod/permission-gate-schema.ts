/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import * as z from "zod";

/**
 * @description A deploy-time gate on a permission-set reference within a profile.\n\nThe gate lives on `PermissionsConfig` rather than on the reference itself\nbecause `PermissionProfile` is `#[serde(transparent)]` and\n`PermissionSetReference` is untagged, so neither can carry a sibling field.
 */
export const PermissionGateSchema = z.object({
    "enabledValue": z.string().describe("Input value for which the permission set stays included"),
"inputId": z.string().describe("Stack input whose resolved value controls the gate"),
"permissionSetId": z.string().describe("ID of the permission set being gated (e.g., \"queue/data-write\")"),
"profile": z.string().describe("Profile name containing the gated reference"),
"resource": z.string().describe("Resource key within the profile (\"*\" or a resource name)")
    }).describe("A deploy-time gate on a permission-set reference within a profile.\n\nThe gate lives on `PermissionsConfig` rather than on the reference itself\nbecause `PermissionProfile` is `#[serde(transparent)]` and\n`PermissionSetReference` is untagged, so neither can carry a sibling field.")

export type PermissionGate = z.infer<typeof PermissionGateSchema>