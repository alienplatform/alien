/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import * as z from "zod";

/**
 * @description Hard ceilings enforced on a sandbox session.\n\nThese are limits, not scheduling requests. Untrusted code does not respect a hint, so every\nfield is enforced by the platform and a platform that cannot enforce one is rejected at plan\ntime rather than silently ignoring it.
 */
export const SandboxLimitsSchema = z.object({
    "cpu": z.string().describe("CPU ceiling in cores or millicores (e.g. `\"1\"`, `\"500m\"`)"),
"disk": z.string().describe("Disk ceiling (e.g. `\"20Gi\"`)"),
"maxProcesses": z.int().min(0).describe("Maximum number of processes, which bounds fork bombs.\n\nOptional because only a container runtime has the primitive: Kubernetes sets a pid ceiling\nper node, not per pod, and neither AWS MicroVMs nor Azure sandboxes expose one. Declaring\nit on a platform that cannot apply it is refused at plan time.").nullish(),
"memory": z.string().describe("Memory ceiling (e.g. `\"2Gi\"`, `\"512Mi\"`)")
    }).describe("Hard ceilings enforced on a sandbox session.\n\nThese are limits, not scheduling requests. Untrusted code does not respect a hint, so every\nfield is enforced by the platform and a platform that cannot enforce one is rejected at plan\ntime rather than silently ignoring it.")

export type SandboxLimits = z.infer<typeof SandboxLimitsSchema>