/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import * as z from "zod";
import { ToolchainConfigSchema } from "./toolchain-config-schema.js";

/**
 * @description Specifies where the sandbox\'s root filesystem comes from.
 */
export const SandboxCodeSchema = z.union([z.object({
    "image": z.string().describe("Image reference (e.g. `ubuntu:24.04`, `ghcr.io/myorg/sandbox:latest`).\n\nTwo backends narrow it in opposite directions: AWS wants an `s3://` bundle, Azure a\nbare catalog name such as `ubuntu`. Each refuses the other's shape while planning."),
"type": z.enum(["image"])
    }), z.object({
    "src": z.string().describe("The source directory to build from"),
get "toolchain"(){
                return ToolchainConfigSchema.describe("Configuration for different programming language toolchains.\nEach toolchain provides type-safe build configuration and auto-detection capabilities.")
              },
"type": z.enum(["source"])
    })]).describe("Specifies where the sandbox's root filesystem comes from.")

export type SandboxCode = z.infer<typeof SandboxCodeSchema>