/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 4856616618d6
 */

import * as z from "zod/v4";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type SandboxBaseImageRepository = {
  /**
   * Manager registry host to `docker login` to and push through.
   */
  registryHost: string;
  /**
   * This project's repository under that host.
   */
  repository: string;
};

/** @internal */
export const SandboxBaseImageRepository$inboundSchema: z.ZodType<
  SandboxBaseImageRepository,
  unknown
> = z.object({
  registryHost: z.string(),
  repository: z.string(),
});

export function sandboxBaseImageRepositoryFromJSON(
  jsonString: string,
): SafeParseResult<SandboxBaseImageRepository, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SandboxBaseImageRepository$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SandboxBaseImageRepository' from JSON`,
  );
}
