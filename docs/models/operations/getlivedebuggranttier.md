# GetLiveDebugGrantTier

How risky an operation is (declared by the plugin metadata).

## Example Usage

```typescript
import { GetLiveDebugGrantTier } from "@alienplatform/platform-api/models/operations";

let value: GetLiveDebugGrantTier = "destructive";
```

## Values

```typescript
"read-only" | "mutating" | "destructive"
```