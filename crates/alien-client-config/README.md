# alien-client-config

Credential loading and client configuration for cloud APIs. Provides `ClientConfigExt` trait for resolving credentials from environment variables and standard credential sources across AWS, GCP, Azure, and Kubernetes. Supports platform-agnostic impersonation.
